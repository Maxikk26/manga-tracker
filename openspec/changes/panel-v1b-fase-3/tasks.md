# Tasks: Add a manga from the panel (`panel-v1b-fase-3`)

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | ≈1635 (±20%), per `design.md` Changed-Lines Forecast |
| 400-line budget risk | High |
| Chained PRs recommended | Yes |
| Suggested split | PR 1 (~395) → PR 2 (~515) → PR 3 (~725) |
| Delivery strategy | resolved by owner: chained |
| Chain strategy | stacked-to-main, sequential |

Decision needed before apply: No — resolved: chained, stacked-to-main
Chained PRs recommended: Yes
Chain strategy: stacked-to-main
400-line budget risk: High

### Suggested Work Units

| Unit | Goal | Likely PR | Focused test command | Runtime harness | Rollback boundary |
|------|------|-----------|----------------------|-----------------|-------------------|
| 1 | Protocol + `intake` skeleton + widened architecture rule + 4 probes + docs correction | PR 1 (→ main) | `uv run pytest tests/test_architecture.py tests/intake tests/discovery/test_covers.py -q` | N/A — no runnable endpoint yet, structural only | Revert `sources/contracts.py`, `intake/`, `tests/test_architecture.py` probes, and the docs correction; nothing downstream depends on it yet |
| 2 | Repo writer + `PastedUrlIntake` + `POST /api/mangas{,/preview}` + label mirror + `cli.py` wiring | PR 2 (→ updated main) | `uv run pytest tests/storage tests/intake tests/web -q` | `curl -X POST http://127.0.0.1:8000/api/mangas/preview -d '{"url":"..."}'` against a locally run `_cmd_panel` | Revert `intake/pasted_url.py`, the two endpoints in `web/app.py`, `cli.py` wiring; PR 1's Protocol/skeleton stay usable by nothing else, so no orphaned callers |
| 3 | Frontend modal + container integration + Vitest coverage | PR 3 (→ updated main) | `npm test` (Vitest) | `npm run build` then manual click-through: paste URL → preview → confirm → grid refetch | Revert the 4 new frontend files + `BookmarkListContainer.tsx`/`styles.css` edits; backend (PR 1+2) keeps working via `curl` with no frontend |

## Slice 1 — Protocol, `intake` skeleton, architecture rule (PR 1, ~395 lines)

- [x] 1.1 Add `fetch_cover(cover_url: str) -> bytes` to the `SourceClient` Protocol in `manga_tracker/sources/contracts.py`. Implements source-client spec "fetch_cover joins the SourceClient Protocol" + "fetch_cover follows the existing error taxonomy". Verify: `uv run pytest tests/discovery/test_covers.py -q` stays green (concrete `ManganatoClient.fetch_cover` behavior is unchanged, only its type is now declared).
- [x] 1.2 Create `manga_tracker/intake/__init__.py` and `manga_tracker/intake/contracts.py`: `MangaIntake` Protocol (`preview`, `confirm`), frozen `AddPreview` (incl. `publication_status_text`) and `AddResult` dataclasses, `InvalidUrl` and `AlreadyTracked(title, status)` exceptions — no Spanish, no HTTP, no source knowledge (design D1/D2). Add `tests/intake/test_contracts.py`: dataclasses are frozen (mutation raises `FrozenInstanceError`), `AlreadyTracked` carries `title`/`status`.
- [x] 1.3 In `tests/test_architecture.py`, widen `DIRECTIONAL_RULES["web"]` from `{"sources.manganato", "notifier.telegram"}` to `{"sources", "notifier", "discovery", "catalogue", "importer", "seed", "intake.pasted_url"}`; add `"intake": {"sources.manganato", "notifier", "discovery", "catalogue", "seed", "web"}`; add `intake`/`web` to the forbidden sets of `sources`/`storage`/`notifier`/`catalogue`. Implements spec.md "`web` never reaches the source, directly or by sequencing it itself" (design D9). Verify: `uv run pytest tests/test_architecture.py::test_directional_boundaries tests/test_architecture.py::test_directional_rules_actually_fire -q`.
- [x] 1.4 Add the four probe files to `test_boundary_check_flags_an_injected_violation` in `tests/test_architecture.py` — `intake/probe.py` (imports `manga_tracker.sources.manganato.client`), `web/probe2.py` (imports `manga_tracker.sources.contracts`), `web/probe3.py` (imports `manga_tracker.intake.pasted_url`), `web/probe4.py` (imports `manga_tracker.sources.manganato`, the spec-mandated one) — and update the expected 7-entry sorted-path list (`catalogue/probe.py, importer/probe.py, intake/probe.py, web/probe.py, web/probe2.py, web/probe3.py, web/probe4.py`); add `intake.pasted_url` to `CONCRETE_IMPLEMENTATIONS`. Implements spec.md "The boundary is proven by an injected violation" (Scenario: fabricated `web`→`sources.manganato` import is flagged). Verify: `uv run pytest tests/test_architecture.py -q`.
- [x] 1.5 Correct `docs/spec-panel-v1b.md` §16 cost row (fase 3: 1 request → 3 requests per add), bump the document version, add a changelog entry and an open-pendings note. Implements design's Migration/Rollout note (PAN §16 predates the cover decision of §128).

## Slice 2 — Repository writer, `PastedUrlIntake`, endpoints, wiring (PR 2, ~515 lines)

- [ ] 2.1 Move the atomic cover write from `discovery/covers.py:145-150` into `manga_tracker/storage/cover_cache.write_cover(cache_dir, manga_id, cover_url, image) -> Path` (`.part`-then-`replace`); update `discovery/covers.py` to call it. Implements design D6 (single copy of "where cover images live on disk"). Add `tests/storage/test_cover_cache.py` covering the atomic write; verify `uv run pytest tests/storage/test_cover_cache.py tests/discovery/test_covers.py -q` stays green.
- [ ] 2.2 Add `MANUAL_ORIGIN = "manual"`, `write_manual_add(conn, *, title, site_id, slug, url, chapters, status, last_chapter_read, cover_url, now) -> tuple[int, int]` and `list_tracked_titles(conn) -> list[tuple[str, str]]` to `manga_tracker/storage/repositories.py`, one transaction writing `mangas`+`manga_sites`+`bookmarks`(+`chapter_history`), `origin='manual'`, `progress_is_approx=0`, `status_changed_at=now`, `detected_via='seed_backfill'`, `last_read_at` NULL. Implements spec.md "The manual bookmark write shape", "Zero chapters is a successful add with a null latest_chapter_num", "Confirm is atomic; any rejection leaves zero rows". Add to `tests/storage/`: `status_changed_at` matches `^\d{4}-\d\d-\d\dT\d\d:\d\d:\d\dZ$`, zero chapters leaves `latest_chapter_num` NULL and writes no `chapter_history`, no `reading_history` row is generated, a mid-transaction failure leaves zero rows across all four tables.
- [ ] 2.3 Create `manga_tracker/intake/pasted_url.py`: `PastedUrlIntake(client: SourceClient, site_id: int, cache_dir: Path)` implementing `MangaIntake`. `preview()`: `extract_slug` (0 req) → gates 1-2 via `find_slug_owner`/`list_tracked_titles`+`matching.slug_variants` (0 req) → `fetch_manga_details` (1 req) → gate 3 via `matching.normalize` → build `AddPreview`. Implements spec.md "Preview validates without writing", "Malformed URL is rejected", "Duplicate active slug is rejected, naming the owner", "Existing terminal title is rejected; reactivation is a PATCH", "Unknown slug is rejected", "Transient source failure requires no automatic retry", "Unexpected source response is rejected" (design D3, D4). Add `tests/intake/test_pasted_url.py` cases for `preview`: gates 1-2 refuse with zero client calls, gate 3 refuses after one call, `NotFound`/`Transient`/`Unexpected` propagate untranslated, `AddPreview` carries title/cover candidate/`publication_status_text`.
- [ ] 2.4 In `manga_tracker/intake/pasted_url.py`, implement `confirm()`: re-run the three gates in-transaction (TOCTOU) → `fetch_chapters` (1 req) → `repo.write_manual_add` (one transaction) → `client.fetch_cover` via `cover_cache.write_cover` (1 req, failure tolerated, outside the transaction) → `AddResult`. Implements spec.md "Cover is cached during the same confirm, no periodic job", "Initial status and chapter validation", "Confirm is atomic; any rejection leaves zero rows" (design D5, D6). Extend `tests/intake/test_pasted_url.py`: happy path writes all four tables, zero chapters is a successful add with NULL `latest_chapter_num`, cover fetch failure leaves the add standing with `cover_cached=False`, a rejected confirm leaves zero rows, `AlreadyTracked` carries the raw status for a terminal and a non-terminal owner.
- [ ] 2.5 In `manga_tracker/web/app.py`, add the Python mirror of the five Spanish status labels from `frontend/src/domain/statusLabels.ts:7` (design D2). Add `tests/web/test_status_labels_parity.py` (~25 lines) parsing `statusLabels.ts` as text and asserting the two maps are equal — implements the drift-pin the design requires ("label mirror + parity test").
- [ ] 2.6 In `manga_tracker/web/app.py`, add request models and `POST /api/mangas/preview` (200, no write) and `POST /api/mangas` (201, writes), both registered before the static mount, both delegating all slug/ficha/chapters sequencing to the injected `MangaIntake` — `web` imports `intake.contracts` only, never `sources`. Implements spec.md "`web` never reaches the source...", "The add form is a modal over the grid" (backend half), and the full Error Taxonomy table in `design.md` (422/409/404/503/502/201 cases, the `existing` sibling key on 409, `terminal` computed server-side). Add `tests/web/test_add_manga_api.py`: one case per taxonomy row, terminal 409 `detail` names title+state+reactivation instruction with `existing.terminal=true`, non-terminal 409 names title+state, preview writes nothing and returns `publication_status_text`, a rejected confirm leaves zero rows in all four tables, 201 returns the `BOOKMARK_KEYS` shape.
- [ ] 2.7 Update `create_app(db_path, intake: MangaIntake)` signature and `cli.py`'s `_cmd_panel`/`_bootstrap` wiring to construct and inject `PastedUrlIntake(client, site_id, cache_dir)`. Implements design D1, D8. Verify: `uv run pytest tests/web -q` and a manual `uv run python -m manga_tracker panel` smoke start.

## Slice 3 — Frontend modal, container integration, Vitest coverage (PR 3, ~725 lines)

- [ ] 3.1 Create `frontend/src/api/http.ts`: `ApiError` (now carrying optional `existing`), `readDetail`, moved out of `bookmarks.ts`; update `frontend/src/api/bookmarks.ts` to import from it and re-export `ApiError`. Implements design's "Frontend" file table. Add/adjust `api/http.test.ts` covering `existing` parsing.
- [ ] 3.2 Create `frontend/src/api/mangas.ts`: `previewManga(url)`, `addManga(body) -> Bookmark`, Spanish network-failure copy per the existing `bookmarks.ts` pattern. Add `frontend/src/api/mangas.test.ts` covering error mapping and `existing` parsing.
- [ ] 3.3 Extend `frontend/src/domain/types.ts`: `MangaPreview` (incl. `publication_status_text`), `MangaAdd` request type, `ExistingManga`. Implements spec.md "Preview validates without writing" (typed contract for the wire shape).
- [ ] 3.4 Create `frontend/src/components/AddMangaModal.tsx`: pure dialog chrome (`<div role="dialog" aria-modal="true">`, not `<dialog>`) + `<form>` (URL required/autofocused, status `<select>` defaulting to `reading`, initial chapter optional `min=0` defaulting to 0, confirm disabled until a preview exists) + preview panel (title, cover `<img>` with `onError` fallback, `publication_status_text` as muted secondary text) + duplicate/terminal affordance (`Ver en «{label}»` button when `existing` is present). Escape/backdrop-click close unless a request is in flight; entrance/exit animation per design, `prefers-reduced-motion` opacity-only. All copy in Spanish. Implements spec.md "The add form is a modal over the grid" (Scenarios: rejection copy names the taxonomy in Spanish). Add `frontend/src/components/AddMangaModal.test.tsx`: preview panel shows title/cover/status text, failed cover candidate falls back, confirm disabled before preview, Escape/backdrop close, busy disables everything.
- [ ] 3.5 Create `frontend/src/containers/AddMangaContainer.tsx`: url/status/chapter state, preview & confirm calls, error message, `existing` state. Implements spec.md "Successful add refreshes the grid", "Abandoning the preview writes nothing". Add `frontend/src/containers/AddMangaContainer.test.tsx`: preview→confirm request bodies, terminal 409 renders the reactivation sentence and its button switches tab and closes modal.
- [ ] 3.6 Modify `frontend/src/containers/BookmarkListContainer.tsx`: add "Agregar manga" button beside `StatusTabs`; on success `await load(false)`, close modal, `setActiveStatus(added.status)`; on a 409 with `existing`, jump to that tab. Implements spec.md "Successful add refreshes the grid" (Scenario). Extend `BookmarkListContainer.test.tsx` (+3 cases): after success the list refetches, modal closes, tab switches; abandoning the preview sends no confirm request and leaves the grid unchanged.
- [ ] 3.7 Modify `frontend/src/styles.css`: modal, backdrop, form, preview panel styles matching the design's Modal UX decisions (200ms entrance / 140ms exit, `cubic-bezier(0.23, 1, 0.32, 1)`, never `scale(0)`, buttons `scale(0.97)` on `:active`). No dedicated test — covered by `AddMangaModal.test.tsx` behavior assertions.
- [ ] 3.8 Run `npm run build` (`tsc --noEmit && vite build`) and `npm test` end to end to confirm the full slice is green before opening PR 3.

## References
- `openspec/changes/panel-v1b-fase-3/specs/panel-add-manga/spec.md`
- `openspec/changes/panel-v1b-fase-3/specs/source-client/spec.md`
- `openspec/changes/panel-v1b-fase-3/design.md`
- `openspec/changes/panel-v1b-fase-3/exploration.md`
