# Tasks: V1a heart phase

> **Mirror drift note (attempt 14)**: this file mirror had fallen out of sync with the authoritative Engram artifact (`sdd/v1a-heart-phase/tasks`, id 86) somewhere around attempts 9-10 — Phases 4-6 below still show mostly `[ ]` even though those units landed and were checked off in Engram across attempts 9-13 (committed through `f453b9e`, plus attempt 13's uncommitted scheduler/Docker work). Reconciling that entire historical drift is out of this attempt's 200-line/three-named-gaps scope; only this attempt's own three items are reflected here (5.7 below, plus the new Phase 7 section). The Engram artifact is the accurate source of truth for Phases 2-6's real completion state until a dedicated reconciliation pass runs.
>
> **RECONCILED 2026-08-02 (code-first pass)**: the dedicated pass above has now run. Every box below was decided by reading the shipped code, tests and runbooks in this repository — not the Engram artifact, not the task text. 19 items flipped to `[x]`; 7 stay `[ ]` with rewritten notes naming exactly what is missing (1.11, 1.12, 1.14, 2.6, 3.11, 6.1, 6.8). Stale "not started / still pending" notes on already-`[x]` items were corrected in place and are marked **SUPERSEDED**. This file is now the accurate mirror; the drift note above is kept only as history. Scope drift worth knowing: the weekly heartbeat (`discovery/heartbeat.py`), the dead-slug notice (`notifier/telegram.py::send_dead_slug_notice`), the startup orphan reaper (`scheduler.py::reap_stale_runs`), the overdue-sweep catch-up (`catch_up_sweep_if_overdue`) and both runbooks all shipped **without a task in this file** — they were added to `docs/one-pager-v1a.md` (v1.7/v1.8/v1.10) after these tasks were written.

> **Size-budget note**: this artifact exceeds the generic 530-word guidance from the `sdd-tasks` skill. Deliberate, disclosed deviation — the change already carries an accepted size exception (`delivery_strategy: exception-ok`, ~880-1500 line forecast, six work units), and the orchestrator's instructions enumerated ~15 mandatory items (AST rules, 9 fixtures, CHECK-constraint traps, D11/D14, unverified-claims checks) that must each land as an explicit task. Compressing further would have dropped required items, mirroring the same disclosed deviation already recorded on `sdd/v1a-heart-phase/spec`.

## Review Workload Forecast

| Field | Value |
|---|---|
| Estimated changed lines | ~880-1500 (per accepted proposal/scope-decision forecast) |
| 400-line budget risk | High |
| Chained PRs recommended | No |
| Suggested split | size-exception single PR; the 6 work-unit commits below double as optional manual slices the user can assemble into separate PRs from commit history |
| Delivery strategy | exception-ok |
| Chain strategy | size-exception |

Decision needed before apply: No
Chained PRs recommended: No
Chain strategy: size-exception
400-line budget risk: High

### Suggested Work Units

| Unit | Goal | Likely PR | Focused test command | Runtime harness | Rollback boundary |
|---|---|---|---|---|---|
| 1 | Source client + project scaffolding | PR 1 | `uv run pytest tests/sources/ tests/test_architecture.py -q` | `uv run python -c "from manga_tracker.sources.manganato.client import build_chapter_url; print(build_chapter_url('one-piece', 145))"` (no network) | Delete `manga_tracker/sources/**`, `pyproject.toml`, `uv.lock`, `tests/**` — nothing else depends on it |
| 2 | Schema + connection factory | PR 2 | `uv run pytest tests/storage/ -q` | `uv run python -c "from manga_tracker.storage.db import connect; connect(':memory:')"` (bootstraps schema, no network) | Delete `manga_tracker/storage/**`, `data/.gitkeep` — independent of unit 1's public API |
| 3 | Seed loader + composition root | PR 3 | `uv run pytest tests/seed/ tests/test_cli.py tests/test_config.py tests/storage/test_ensure_site.py -q` | `uv run python -m manga_tracker seed --file seed-plantilla.csv --dry-run` (real, unblocked as of attempt 6 — validates and writes nothing but the `sites` bootstrap row) | Delete `manga_tracker/seed/**`, `cli.py`, `__main__.py`, `config.py`, `logging_setup.py`, `.env.example`, `seed-plantilla.csv` — needs units 1+2 present but removable without touching them |
| 4 | Discovery core + `feed_check` + `active_sweep` | PR 4 | `uv run pytest tests/discovery/ -q` | `uv run python -m manga_tracker run-job feed_check` (requires configured env + live source — real bring-up command) | Delete `manga_tracker/discovery/**`, `manga_tracker/notifier/contracts.py` — needs units 1+2, independent of unit 5's concrete sender |
| 5 | Telegram digest emitter | PR 5 | `uv run pytest tests/notifier/ -q` | `uv run python -m manga_tracker test-telegram` | Delete `manga_tracker/notifier/telegram.py` — independent of units 1-4 beyond the `DigestSender` contract |
| 6 | Scheduler + Docker | PR 6 | `uv run pytest tests/scheduler/ -q` | `docker compose up` then `uv run python -m manga_tracker run` | Delete `scheduler.py`, `Dockerfile`, `docker-compose.yml` — removes deployment only, no application code touched |

Threat matrix: N/A per design (no routing/shell/subprocess/VCS/PR-automation boundary in this change) — no separate RED-test-per-threat-case is required beyond the tests already listed below.

## Phase 1: Source client + project scaffolding (Unit 1)

*Stands alone: zero DB dependency, testable first against trimmed fixtures.*

- [x] 1.1 Create `pyproject.toml` with direct deps `curl-cffi>=0.13,<0.15` (D11 cap), `beautifulsoup4`, `APScheduler` 3.x; `pytest` dev-only; generate and version `uv.lock`. **Deviation**: pinned `curl-cffi>=0.15` instead — see 1.4.
- [x] 1.2 Verify D11 holds: `uv sync` then `uv pip list` confirms no `rich`/`markdown-it-py`/`mdurl`/`Pygments` entered the closure. Holds for `<0.15`; does not hold for the actually-shipped pin (see 1.4).
- [x] 1.3 Verify Unverified claim #2 (design): pinned `curl-cffi` with `impersonate="chrome"` (targets 131/124) gets a clean 200 from the canonical manganato host. Confirmed live: 200, both named targets present.
- [x] 1.4 Check Unverified claim #3: no open libcurl-impersonate advisory against the pinned version at pin time. **FALSE** — GHSA-qw2m-4pqf-rmpp (SSRF, HIGH, CWE-918) open on 0.13.x/0.14.x, fixed only in 0.15.0. Per D11's own rule an advisory outranks the cap: cap lifted to `>=0.15`.
- [x] 1.5 Create package skeleton: `manga_tracker/__main__.py`, `cli.py`, `config.py`, `logging_setup.py` — landed as real implementations in unit 3 attempt 6 (see 3.1-3.5), not stubs. `clock.py` still not created — no consumer needs an injectable clock until the scheduler/discovery jobs (units 4/6) exist.
- [x] 1.6 Create `manga_tracker/sources/contracts.py`: `Response` dataclass, `Transport` Protocol, `FeedItem`, `Chapter`, `MangaDetails`, `SourceClient` Protocol, `NotFound`/`Transient`/`Unexpected`.
- [x] 1.7 Create `manga_tracker/sources/manganato/transport.py`: curl-cffi call normalized into `Response`; confine `curl_cffi` import to this file only.
- [x] 1.8 Create `manga_tracker/sources/manganato/parsing.py`: SRC §2/§4 selectors; ad filter via `any(c.startswith("js-banner-") for c in el.get("class", []))` (not a CSS `^=` match — that only fires when the banner class is first); confine `bs4` import to this file only.
- [x] 1.9 Create `manga_tracker/sources/manganato/client.py`: `fetch_latest_feed`, `fetch_chapters`, `fetch_manga_details`, `build_chapter_url`, `BASE_URL` — all done across attempts 2 (JSON path) and 3 (HTML path). **Contracts fix**: `MangaDetails` was `title`/`source_key`/`url` (never matched CD Operacion 3's actual shape); corrected to `title`/`cover_url`/`publication_status_text`/`last_updated_text`.
- [x] 1.10 Referer, no-retry-on-NotFound (attempt 2) plus no-delay-before-the-isolated-feed-call (attempt 3, structural: `fetch_latest_feed` makes one call on a transport instance, and `CurlCffiTransport`'s delay only fires from a transport's 2nd+ call) — all landed.
- [x] 1.11 Create `tests/conftest.py`: autouse session fixture patching `socket.socket.connect` to raise (blocks manganato and Telegram alike); injected `Transport`/`sleeper`/`rng` fixtures. **Still partial, verified 2026-08-02.** Done: `tests/conftest.py` contains exactly one fixture, the autouse `block_network_sockets`, which monkeypatches `socket.socket.connect` to raise. Missing: no shared `Transport`/`sleeper`/`rng` fixtures exist anywhere. `FakeTransport` is still local to `tests/sources/test_client.py`, and every other suite rolls its own double — `FakeClient` in `tests/seed/test_loader.py`, `tests/discovery/test_feed_check.py` and `tests/discovery/test_links.py`, `FakeSender`/`CapturingSender` in `tests/discovery/test_runs.py`, `FakeApi` in `tests/notifier/test_telegram.py`. The injection points exist in production code (`CurlCffiTransport.__init__(sleeper=, rng=)`, `TelegramSender.__init__(sleeper=)`), and as of 2026-08-04 both `tests/sources/test_transport.py` and `tests/sources/test_sitemap.py` do inject `sleeper`/`rng` (see 1.14) — but each rolls its own local recorder, so what is still missing here is the *shared* fixture, not the injection. **CERRADO como no-objetivo 2026-08-05.** La mitad que importaba está hecha: `conftest.py` bloquea sockets para toda la suite. Los fixtures compartidos de `Transport`/`sleeper`/`rng` se declinan: seis suites tienen su propio doble, cada uno ajustado a lo que ese archivo prueba —el de `test_transport.py` guarda las llamadas a `rng`, el de `test_kitsu.py` expresa fallos HTTP, el de `test_active_sweep.py` responde por slug— y un fixture único tendría que ser la unión de los tres. Es refactor sin defecto que lo motive. Se reabre si un doble nuevo necesita de verdad lo mismo que otro.
- [x] 1.12 Create the 9 trimmed fixtures under `tests/fixtures/`. **Done 2026-08-04.** `tests/fixtures/chapters_empty.json` is written — `success: true`, `chapters: []`, `pagination.total: 0` — and the directory now holds it alongside the other eight named below (plus seven more that arrived with the sitemap and Kitsu work). Its values are chosen to be mutation-sensitive rather than merely representative: `success: true` with an *empty list* is the one payload that distinguishes a type check from a truthiness check, so replacing `isinstance(data.get("chapters"), list)` with `not data.get("chapters")` turns D14 into `Unexpected` and the test catches it. **Two tests feed on it, and they assert opposite consequences on purpose.** `tests/sources/test_client.py::test_fetch_chapters_empty_array_is_a_success_not_an_error` pins the client returning `[]` rather than raising — the behaviour every caller below depends on and which nothing asserted before. `tests/discovery/test_active_sweep.py::test_an_empty_chapters_array_is_a_success_that_resets_the_counter` pins the sweep side: `consecutive_failures` resets to 0 (from 4, one short of the threshold, so a misclassification also fires the dead-slug notice and fails twice over), no `chapter_history` row, `latest_chapter_num` untouched, run `ok` with 0 notifications. **That side turned out genuinely uncovered**: `test_dead_slug_counter`'s "success-resets" case only ever passed a *non-empty* list, so the reset was untested for exactly the payload the rule exists for. The loader side needed nothing — `tests/seed/test_loader.py::test_zero_chapters_row_reported_and_discarded_whole` already covers the discard-whole divergence, so no duplicate was added. Proven by a 6-mutation pass (list check made truthy; empty folded into the false-success guard so it raises `NotFound`; a synthetic chapter substituted for the empty list; the counter reset moved after the empty-array check; the empty array counted as a failure; the guard weakened to `chapters is None`) — **all 6 killed.** Superseded note kept for history: **was 8/9, verified 2026-08-02** by listing the directory: `chapters_ok.json`, `chapters_false_success.json`, `chapters_missing_array.json`, `feed_page.html`, `feed_page_ads_only.html`, `feed_page_structure_changed.html`, `feed_item_no_number.html`, `manga_details.html`. `chapters_empty.json` (D14 empty-array-success) is the one absentee. As of 2026-08-04 it is no longer blocked on anything: the transport-retry/`Transient` work it was waiting on landed with 1.14, so this fixture and the `fetch_chapters`-returns-`[]` test it feeds are the whole of what remains open here.
- [x] 1.13 Write `tests/sources/test_parsing.py`: happy path + ad filter + `data-src` preference + raw date hint in one test, zero-real-items → Unexpected (parametrized over ads-only and renamed-container), unparseable chapter text dropped, `fetch_manga_details`'s four fields.
- [x] 1.14 `test_client.py` now also covers `fetch_latest_feed`/`fetch_manga_details` (happy path + 404). **Done — the item's own text was already satisfied, and gap (b) of the 2026-08-02 audit is closed 2026-08-04.** `tests/sources/test_client.py` (5 tests) covers `fetch_chapters` happy path with newest-first ordering and the organic `Referer`, false-success and 404 both raising `NotFound`, a payload missing `data.chapters` raising `Unexpected`, plus `fetch_latest_feed` and `fetch_manga_details` (happy path + 404). **Gap (b), the missing transport test, is closed by `tests/sources/test_transport.py` (27 tests)**: no delay before the first request and one before every later request, the delay drawn from `rng` inside the literal 5-15 s window (the bounds are asserted as numbers, not imported from the module under test, so moving a constant cannot move the assertion with it), Chrome impersonation and the 30 s timeout asserted on the call that actually reaches `curl_cffi`, one retry after a 30 s wait parametrized over all six codes in `TRANSIENT_STATUS_CODES`, a transient status surviving the retry returned as data instead of raised, a non-transient 404 returned on the first attempt, and a network error retried once then propagating as `Transient` with its cause chained. `curl_get` is patched at the transport's own call site and `sleeper`/`rng` injected, so nothing touches a socket or waits. Suite 303 -> 330. Proven by a 33-mutation pass over `transport.py` (delay before the first request; no delay at all; hardcoded delay; `rng` consulted but ignored; each bound moved, inline and as a constant; the per-instance "already requested" flag promoted to class state; three attempts; no retry; no pre-retry wait; the retry paying a courtesy draw; every status retried; 404 added to the transient set and 403/429/504 each dropped; the second exception swallowed into an empty `Response`; the cause unchained; impersonation, timeout, headers and url each dropped or altered; curl's own object returned unnormalized; response headers aliased instead of copied) — **all 33 killed, no survivors.** Two side-effects worth knowing: `pyproject.toml` needed `addopts = "--import-mode=importlib"` because `tests/catalogue/test_transport.py` and `tests/sources/test_transport.py` share a basename, which pytest's default `prepend` mode refuses; and gap (a), D14 empty-array-success, is **not** closed here — it now belongs wholly to 1.12 together with the fixture it needs.
- [x] 1.15 Write `tests/sources/test_build_chapter_url.py`: parametrized `80`, `'80.0'`, `145.0`, `45.5`. Done; "assert zero requests made" is covered by the autouse socket-blocking fixture rather than a dedicated assertion, since `build_chapter_url` takes no `Transport` to begin with.
- [x] 1.16 Create `tests/test_architecture.py` with rule 1 (`sources/**` imports nothing from storage/discovery/notifier/seed) and the `curl_cffi`/`bs4` confinement half of rule 5. Went further: implemented all 5 rules now (see 2.9/3.13/4.12/5.9/6.12) since the directional/confinement checks don't need the target packages' production files to exist.

## Phase 2: Schema + connection factory (Unit 2)

*Stands alone: `ensure_schema` takes no arguments — testable with no client and no `cli.py`.*

- [x] 2.1 Create `manga_tracker/storage/schema.sql`: 7 tables (`mangas`, `sites`, `manga_sites`, `bookmarks`, `reading_history`, `chapter_history`, `job_runs`) + the single UPDATE-only `reading_history` trigger; all `CREATE ... IF NOT EXISTS`.
- [x] 2.2 In `schema.sql`, set CHECK constraints: `job_runs.job_name` allows `feed_check`, `active_sweep`, `onhold_sweep` (onhold_sweep out of scope this phase but required now to avoid migrating a populated DB later); `chapter_history.detected_via` allows `feed`, `active_sweep`, `onhold_sweep`, `seed_backfill`.
- [x] 2.3 In `schema.sql`, add the closed index set: `bookmarks(status)`, `job_runs(job_name, started_at)`, `reading_history(manga_id, read_at)`, `reading_history(read_at)`, UNIQUEs on `sites(name)`, `mangas(kitsu_id)`, `manga_sites(manga_id, site_id)`, `manga_sites(site_id, source_key)`, `bookmarks(manga_id)`, `chapter_history(manga_site_id, chapter_num)`.
- [x] 2.4 Add `manga_sites.consecutive_failures`: non-nullable, default 0.
- [x] 2.5 Create `manga_tracker/storage/db.py`: `connect(path)` sets `PRAGMA foreign_keys=ON`, WAL + `busy_timeout` at bootstrap, then calls `ensure_schema(conn)` before returning; `ensure_schema(conn)` runs `schema.sql` standalone (no other args, independently testable); `transaction(conn)` context manager. (`ensure_site` lands in unit 3 with its first caller, per the no-helper-before-caller rule.)
- [x] 2.6 Create `manga_tracker/storage/repositories.py`: parameterized query helpers for `mangas`/`manga_sites`/`bookmarks`/`chapter_history`/`job_runs` used by seed and discovery — no slug or user value ever interpolated into SQL. **Bookkeeping-open, not code-open — do NOT re-implement.** Done: `repositories.py` ships `find_manga_site_by_slug` and `write_seed_backfill`, fully parameterized, covering `mangas`/`manga_sites`/`bookmarks`/`chapter_history` for the seed path. Not as written: no `job_runs` helper lives in this module, and discovery never imports `repositories` at all — the only importer is `seed/loader.py:12`. The `job_runs` lifecycle shipped elsewhere by design: `discovery/runs.py` (`open_run`/`close_run`) and `scheduler.py` (`_close_if_still_open`, `reap_stale_runs`, `sweep_is_overdue`), with `feed_check.py`/`active_sweep.py`/`links.py`/`heartbeat.py` each running their own inline SQL against a duck-typed `conn` (deliberate — it keeps `sqlite3` confined to `storage/`, per `test_architecture.py`). The functional requirement holds everywhere: every statement in those modules is parameterized, no slug or user value is interpolated. What remains is a decision, not work — either record this as an accepted deviation (discovery owns its own queries; `repositories.py` is the seed's) or move the `job_runs` helpers here. **CERRADO como desviación registrada 2026-08-05.** No se implementa como está escrito, y el motivo es que la prescripción era la equivocada: los helpers de `job_runs` viven en `discovery/runs.py` y `scheduler.py`, junto a la lógica que los usa, y `discovery` nunca importa `repositories`. Todo el SQL está parametrizado, que es la propiedad que esta tarea existía para garantizar. Moverlos ahora sería un refactor sin cambio de comportamiento sobre código en producción. Los de `mangas`/`manga_sites`/`bookmarks`/`chapter_history` sí están en `repositories.py`, más `find_slug_owner` y los tres escritores del import.
- [x] 2.7 Create `data/.gitkeep` so the mount point exists before any run.
- [x] 2.8 Write `tests/storage/test_schema.py` against a real DB (`:memory:`): schema creation is idempotent (run twice); FK violation raises and cascade delete works (proves the pragma); trigger fires on UPDATE and NOT on INSERT; downward `last_chapter_read` correction captured as a negative-delta event; CHECK constraint rejects an unknown `status` value; every DM index exists. **Partial**: the `chapter_history` re-insert-is-a-silent-no-op scenario from this task's original wording is not yet covered by a dedicated regression test — the UNIQUE index enabling that no-op (`idx_chapter_history_manga_site_chapter`) is in place and tested for existence, and attempt 5's `write_seed_backfill` exercises `INSERT OR IGNORE` in practice.
- [x] 2.9 Extend `tests/test_architecture.py`: add rule 3 (`storage/**` imports nothing from sources/discovery/notifier/seed) and the `sqlite3`-confined-to-`storage/` half of rule 5. Done in unit 1 (see 1.16 note); still passes unmodified with `repositories.py`/`config.py`/`logging_setup.py`/`cli.py` added.

## Phase 3: Seed loader + composition root (Unit 3, needs 1+2)

*Stands alone: first executable path; standalone test suite passes without units 4-6.*

> **Attempt 6 status**: the composition root landed — `ensure_site`, `config.py`, `logging_setup.py`, `cli.py`/`__main__.py`, `.env.example`. `seed` is fully wired end to end (`--dry-run` included). ~~**Still deferred** (discovery/notifier don't exist yet): `run`, `test-telegram`, `run-job` subcommands in `cli.py`~~ — `config.require_telegram()`, the fail-fast gate they will all call, is built and tested directly now so it doesn't block units 4-6.
>
> **SUPERSEDED 2026-08-02**: the deferral struck through above is closed. All four subcommands — `seed`, `run`, `run-job {feed_check,active_sweep,heartbeat}` and `test-telegram` — are registered in `cli.py::build_parser` (lines 106-121) and `require_telegram` gates the three that send.

- [x] 3.1 Add `ensure_site(conn, name, base_url)` to `manga_tracker/storage/db.py`: `ON CONFLICT(name) DO UPDATE SET base_url=excluded.base_url, updated_at=excluded.updated_at RETURNING id` (not `INSERT OR IGNORE` — must refresh a stale host per SRC §9 playbook). Tested in `tests/storage/test_ensure_site.py` (task 3.12).
- [x] 3.2 Create `manga_tracker/config.py`: `load_config()` reads `os.environ` into frozen dataclasses (`AppConfig`/`TelegramConfig`); `seed` never requires `TELEGRAM_BOT_TOKEN`/`TELEGRAM_CHAT_ID` — `require_telegram(config)` is the separate fail-fast gate a sending subcommand calls, naming every missing var in one `SystemExit`. Tested in `tests/test_config.py`.
- [x] 3.3 Create `manga_tracker/logging_setup.py`: stdlib `logging` via `logging.basicConfig` to stdout, plain text UTC (`Formatter.converter = time.gmtime`), `LOG_LEVEL` env; a filter defaults `job_name`/`run_id` to `-` outside a run; `run_logger(job_name, run_id)` returns the per-run `LoggerAdapter` (design D8) — not yet exercised by a caller since no job (unit 4) opens a `job_runs` row yet.
- [x] 3.4 Create `manga_tracker/cli.py` (the only composition root) + `manga_tracker/__main__.py`: argparse entrypoint wiring **`seed` only** this attempt; every invocation opens a connection (`connect`, which calls `ensure_schema`) and calls `ensure_site(conn, "manganato", client.BASE_URL)`. ~~**Partial, disclosed**: `run`/`test-telegram`/`run-job` need discovery/notifier/scheduler (units 4-6) and are not wired yet~~ — **SUPERSEDED 2026-08-02**: all three are wired (`cli.py::_cmd_run`, `_cmd_run_job`, `_cmd_test_telegram`; subparsers at `cli.py:111-121`), each calling `config.require_telegram()` before constructing a `TelegramSender`. See 6.5 and 7.1.
- [x] 3.5 Create `.env.example` documenting every env var `config.py` consumes this attempt: `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID`, `DB_PATH`, `LOG_LEVEL`.
- [x] 3.6 Create `manga_tracker/seed/loader.py`: validate-all-rows-then-report before any write; slug extracted from the segment after `/manga/` (tolerates www/trailing slash/query/fragment) from either ficha or chapter URL; `last_chapter_read` taken only from its CSV column. **Partial**: non-blocking warnings and the duplicate-slug-in-file error are deferred (disclosed in the module docstring) — any blocking error currently halts the whole load, no skip-invalid override yet. Note: the client's `extract_slug`/`build_manga_url` (moved onto the `SourceClient` contract during gatekeeping, after this task was first written) are what the loader now calls — it holds no URL-shape knowledge itself, asserted by `test_loader_holds_no_source_url_knowledge`.
- [x] 3.7 In `loader.py`, implement per-row load via `storage.repositories`: create/find `mangas` → create `manga_sites` (slug + canonical reconstructed URL) → `fetch_chapters` sets `latest_chapter_num`/url/at + `last_checked_at`, writes `chapter_history` with `detected_via=seed_backfill` → create `bookmarks` (`origin=seed`, `progress_is_approx=0`). Implemented as `repositories.write_seed_backfill`.
- [x] 3.8 In `loader.py`, implement D14 for seed: a well-formed empty `chapters` array reports the row and discards it completely (no `mangas`/`manga_sites`/`bookmarks`) — same treatment as a 404 row (and `NotFound`/`Unexpected` from `fetch_chapters` also discard the row and continue).
- [x] 3.9 In `loader.py`, implement re-run safety: `repositories.find_manga_site_by_slug` reuses an existing manga/mapping by slug, `write_seed_backfill` updates the bookmark from the file, `chapter_history`'s UNIQUE index makes the dump idempotent (`INSERT OR IGNORE`), and the schema's own trigger `WHEN` clause (unit 2) already guarantees no `reading_history` event when `last_chapter_read` is unchanged — no extra loader logic needed for that last guarantee. Not yet covered by a dedicated re-run test this attempt (disclosed).
- [x] 3.10 Create `seed-plantilla.csv` at the repo root (header + 2 example rows clearly marked `EXAMPLE - DELETE THIS ROW`) — exact name required by `.gitignore`'s re-inclusion rule.
- [x] 3.11 Write `tests/seed/test_loader.py`: every SEED error/warning, slug extraction across ficha/chapter/www/trailing-slash/query/fragment, progress never derived from URL, re-run idempotency, zero-chapter slug reported and discarded whole, not-found/unexpected row fully discarded. **Done 2026-08-04, and it needed production code first — the missing tests were missing because the validations were.** `manga_tracker/seed/loader.py` now implements SEED "Validacion" in full: the two file-level **errors** (a slug repeated inside the file; a slug the database already gave to another manga, via the new `repositories.find_slug_owner`) and all three **warnings** (a title spread over different slugs; a `reading` row with no chapter; more than 30 rows). Warnings print and load; errors still block the whole file, and everything prints before any write. The module docstring no longer claims them deferred — what it now discloses is the one thing still unbuilt, the skip-invalid override SEED offers as an alternative. **One decision was required and is recorded, not invented**: SEED said "slug que en la base ya apunta a otro manga" without saying what "otro" is measured against. The file has no identity but the title, so the comparison is against the stored title — *except* when that manga carries a `kitsu_id`, because the same spec says Kitsu may replace the typed title and the importer replaces `mangas.title` for every entry it matches, so a strict comparison would reject most of the file after the first import and block the re-run SEED calls safe. Closed as a decision in **`docs/spec-seed-manual.md` v2.4** (changelog + open-pendings updated), with the pins of both dependents refreshed per README's map: `docs/runbook-deploy.md` and `docs/spec-importador-kitsu.md`. The >30-row warning was checked against the file that is fed back through this loader — KIT measures its pending list at **5 rows** (3 failed matches + 2 without a catalogue mapping), so nothing had to be carved out for it, and the test asserts both 30 (silent) and 31 (warns) so it cannot creep down onto that file. 12 new tests: repeated slug blocks (and one row does not), the same row twice reported once as the slug error rather than also as a title warning, the database conflict blocks, the matching-title re-run loads, the catalogue-owned title exempted, warnings reported without blocking, a non-`reading` chapterless row silent, the 30/31 boundary, `NotFound`/`Unexpected` discarded-and-continue (parametrized, asserting the *next* row still loads), and re-run idempotency asserting `reading_history` stays empty on an identical re-run and records exactly one 5→7 event when progress moves. Proven by a 22-mutation pass over `loader.py`/`repositories.py`/`schema.sql` — **all 22 killed** (list at the bottom of this file). Superseded note kept for history: **was partial, verified 2026-08-02.** Landed in `tests/seed/test_loader.py` (5 tests): slug extraction across ficha/chapter/www/trailing-slash/query/fragment (`test_extract_slug_tolerates_ficha_and_chapter_url_variants`), progress never derived from URL (`test_loader_holds_no_source_url_knowledge`, an AST-invisible string check on `loader.py`), a blocking error halting the load and writing nothing (`test_invalid_row_blocks_entire_load_and_writes_nothing`), D14 zero-chapter slug reported and discarded whole (`test_zero_chapters_row_reported_and_discarded_whole`), and per-row progress printed before each request (`test_load_prints_progress_per_row_before_its_request`). Dry-run-blocks-write lives in `tests/test_cli.py::test_seed_dry_run_succeeds_without_telegram_env`. Still missing: non-blocking warnings and duplicate-slug-in-file (**both still missing from `loader.py` itself** — its module docstring still reads "Deferred: warnings, skip-invalid override, duplicate-slug-in-file check", so these need production code before a test, and a blocking error still halts the whole load with no skip-invalid override); `NotFound`/`Unexpected` row discarded-and-continue (the `except (NotFound, Unexpected)` branch at `loader.py:76-78` has no test); and re-run idempotency. (`ensure_site`-refresh landed instead in its own file, 3.12.)
- [x] 3.12 Write `tests/storage/test_ensure_site.py`: `ensure_site` inserts then refreshes `base_url` on conflict (single row, same id, new `base_url`).
- [x] 3.13 Extend `tests/test_architecture.py`: add the `seed/**` half of rule 4 (may import `sources.contracts` but never `sources.manganato`). Done in unit 1 (see 1.16 note) — verified unmodified against `seed/loader.py`, `storage/repositories.py`, and this attempt's new `cli.py`/`config.py`/`logging_setup.py` (which sit outside the restricted packages by design, so no rule change was needed for the composition root).
- [x] 3.14 Verify with `git check-ignore --stdin`: `seed-plantilla.csv` is versioned (exit 1, not ignored), `data/seed.csv` and `data/manga-tracker.db` are ignored (exit 0 each, matched by `.gitignore:31:*.csv` and `.gitignore:20:data/*` respectively). `.env.example` also verified versioned (exit 1) this attempt.

## Phase 4: Discovery core + `feed_check` + `active_sweep` (Unit 4, needs 1+2)

*Stands alone against a fake `DigestSender` — precedes unit 5.*

> **Attempt 8 status**: the 200-line hard ceiling for this attempt again forced a scope cut, per the user's mandated priority ("run bookkeeping and notify-before-update first, then `active_sweep` with the dead-slug counter, then `feed_check`"). `notifier/contracts.py` + `discovery/runs.py` (`open_run`/`close_run`/overlap guard/`send_and_advance`) landed complete and tested (133 authored lines). `active_sweep.py` + its dead-slug-counter test were drafted, measured at ~111 additional lines together (even after trimming — the schema's FK chain, mangas→sites→manga_sites→bookmarks, makes a real seeded test irreducibly ~45+ lines), pushing the attempt to ~246 total; reverted whole to stay under budget rather than shipped half-tested or under-reviewed. `feed_check.py` and `links.py` were never started (lower priority still). Nothing here is a design deviation; it is an explicitly disclosed, budget-forced scope cut — attempt 7's same pattern repeating for the same reason (this phase's total scope does not fit one 200-line attempt).
>
> **Attempt 9 status**: `active_sweep.py` (unit-4c) landed complete and tested at 199/200 authored lines (81 production + 118 test) — the exact item cut from attempt 8, re-added near-verbatim per that attempt's own recommendation. Population query (`reading`/`want_to_read`, `consecutive_failures < 5`), dead-slug counter (NotFound +1 / Transient untouched / any success → 0), D14 empty-response-is-success, the "only newest compared, rest still written to `chapter_history`" rule, and full `open_run`/`send_and_advance`/`close_run` wiring are all done and tested. ~~`feed_check.py` and `links.py` remain the only Phase-4 items not started.~~
>
> **SUPERSEDED 2026-08-02**: both landed. `manga_tracker/discovery/feed_check.py` and `manga_tracker/discovery/links.py` exist, are wired into the scheduler and the digest path respectively, and have their own test files (`tests/discovery/test_feed_check.py`, `tests/discovery/test_links.py`). Phase 4 has no unstarted items. `discovery/heartbeat.py` also shipped here without a task — see the reconciliation note at the top of this file.

- [x] 4.1 Create `manga_tracker/notifier/contracts.py`: `DigestLine` dataclass, `DigestSender` Protocol (`send_digest(lines) -> bool`, all-or-nothing) — discovery builds lines without importing the concrete sender. Landed this attempt (16 lines).
- [x] 4.2 Create `manga_tracker/discovery/detection.py`: the shared rule, implemented once — (1) seal `last_checked_at` always; (2) bookmark-state gate: `completed`/`dropped` stop here, no history, no update; (3) compare vs `latest_chapter_num` (strictly lower ⇒ log only, never write backward; lower-or-equal ⇒ done); (4) `chapter_history` INSERT OR IGNORE regardless of notification; (5) branch: `on_hold` updates immediately and silently, active accumulates with `latest_chapter_num` untouched until send succeeds. Done as `apply_detection(conn, mapping, chapter, *, detected_via, now, logger) -> Candidate | None`, with `Mapping`/`Candidate` as the module's own plain types (no dependency on `notifier.contracts` — `Candidate` carries everything `runs.send_and_advance` needs).
- [x] 4.3 Create `manga_tracker/discovery/links.py`: link resolution hierarchy — (1) real URL of the first unread chapter from `chapter_history` if registered, (2) `build_chapter_url` pattern guess, (3) newest chapter's URL. **Done, verified 2026-08-02**: `discovery/links.py::resolve_link` implements the three steps exactly — lowest `chapter_history.chapter_num > last_chapter_read` with a non-null `chapter_url` wins; a registered row with a null URL falls to `client.build_chapter_url(source_key, chapter_num)`; null progress or nothing registered above it falls to `newest_url`. Tested by `tests/discovery/test_links.py` (4 cases, one per step plus null progress, each asserting `client.calls` so a step cannot be reached by accident). Reached on the production path via `discovery/runs.py::_digest_url`, asserted by `tests/discovery/test_runs.py::test_digest_link_comes_from_the_resolution_hierarchy_not_the_detected_chapter` — added precisely because the function once had full unit coverage while nothing called it.
- [x] 4.4 Create `manga_tracker/discovery/runs.py`: opens/closes one `job_runs` row per run (`status` ok/error/partial, counts, `error_summary`); overlap guard (no new run of the same `job_name` while one is open, raises `RunAlreadyOpen`); `send_and_advance` (notify-before-update). Landed this attempt (52 lines) as `open_run`/`close_run`/`send_and_advance`; does not import `sqlite3` (confined to `storage/`, matching `detection.py`'s existing pattern — `conn` is duck-typed). ~~**Not yet exercised by a real job function on a worker thread**~~ — **SUPERSEDED 2026-08-02**: `scheduler.py::_make_job` now opens exactly one `connect(db_path)` per run inside the job function, on the executor's worker thread (sqlite3 defaults to `check_same_thread=True`, so it cannot be built elsewhere), and closes it in `finally`. Exercised by `tests/scheduler/test_registration.py::test_run_job_once_dispatches_exactly_one_job` and `::test_job_wrapper_closes_the_run_row_and_logs_when_the_job_body_raises` against a real on-disk DB.
- [x] 4.5 Create `manga_tracker/discovery/feed_check.py`: one `fetch_latest_feed` call, match by (site, `source_key`), `detected_via=feed`, `source_published_at` always null. **Done, verified 2026-08-02**: `discovery/feed_check.py::feed_check` makes exactly one `client.fetch_latest_feed()` call per run; `_mapping_for` matches on `ms.site_id = ? AND ms.source_key = ?`; `DETECTED_VIA = "feed"` is a separate constant from `JOB_NAME = "feed_check"` on purpose (the two CHECK constraints take different value sets, and reusing one would make every `INSERT OR IGNORE` into `chapter_history` silently drop); and `Chapter(..., published_at=None)` forces `source_published_at` NULL, never passing `item.updated_at_hint`. Tested by `tests/discovery/test_feed_check.py` — unmatched item ignored with `status='ok', updates_found=0`; matching active item produces a candidate and advances `latest_chapter_num`; `source_published_at` asserted NULL.
- [x] 4.6 Create `manga_tracker/discovery/active_sweep.py`: daily loop over `reading`/`want_to_read` mappings with a slug (excluding paused mappings), `fetch_chapters` per mapping (delay lives in `transport.py`, confirmed — no sleeper needed at this layer), compares only the newest via the shared rule, `detected_via=active_sweep`; implement D14 — an empty `chapters` array is a **success** (resets `consecutive_failures`, nothing else happens). Landed attempt 9 (81 lines) as `active_sweep(conn, client, sender, *, now, logger)`, plus the remaining up-to-50 returned chapters written to `chapter_history` idempotently (`INSERT OR IGNORE`) — the sub-item attempt 8 cut from its own draft is included here.
- [x] 4.7 In `active_sweep.py`, implement the dead-slug counter: `consecutive_failures` +1 on NotFound only, reset to 0 on ANY success (including D14's zero-chapter success), mapping at ≥5 skipped with zero request consumed. Landed attempt 9; the population query filters `consecutive_failures < 5` directly in SQL, so a paused mapping never reaches `fetch_chapters`.
- [x] 4.8 Wire notify-before-update across `feed_check.py`/`active_sweep.py`: accumulate active-manga candidates, call `DigestSender.send_digest`; success advances `latest_chapter_*` for every included mapping and closes `job_runs` ok; failure advances nothing, closes `partial`; zero candidates = no send. **`active_sweep.py`'s wiring is DONE** (attempt 9: `send_and_advance` + `close_run` called at the end of every run, tested). ~~**Still pending**: the `feed_check.py` side (4.5 not started).~~ **SUPERSEDED 2026-08-02**: the `feed_check.py` side is done too — `feed_check.py::_check` accumulates candidates, calls `send_and_advance`, and closes the run `"partial" if outcome.failed else "ok"` with `notifications_sent=outcome.sent`; its own `except BaseException` wrapper closes the row `error` and re-raises. Both sides of this task are now closed.
- [x] 4.9 Write `tests/discovery/test_detection.py` against a fake `DigestSender`: notify-before-update regression (failing send ⇒ no `latest_chapter_num` moved, `job_runs.status='partial'`, `chapter_history` still written); lower number ⇒ logged, never backwards; terminal bookmarks consume zero requests and write no `chapter_history` row. **Reallocated, not skipped**: `test_detection.py` (unchanged from attempt 7) covers the rule-only scenarios (terminal/lower-number/on_hold/idempotency); the failing-send ⇒ `job_runs.status='partial'` regression — which needs `runs.py`, not just the rule — landed in `tests/discovery/test_runs.py` instead (task 4.11), a more accurate home since it exercises `send_and_advance`/`close_run` together. Both files together satisfy this task's full scenario list.
- [x] 4.10 Write `tests/discovery/test_active_sweep.py`: `consecutive_failures` +1 on not-found only, reset on any success including zero-chapter, skipped at ≥5. Landed attempt 9 (118 lines, 6 test executions — 3 parametrized dead-slug-counter cases, 1 threshold-skip, 1 failing-send-closes-partial regression, 1 non-newest-chapters-recorded), all passing.
- [x] 4.11 Write `tests/discovery/test_runs.py`: overlap guard behavior (`RunAlreadyOpen` raised on a second `open_run` of the same `job_name`); notify-before-update (zero candidates sends nothing; failed send advances nothing and closes `partial`; successful send advances every candidate and records `job_runs.notifications_sent`). Landed this attempt (65 lines, 4 test functions/cases, all passing). ~~**Not yet covered**: "one connection per run opened on the worker thread"~~ — **SUPERSEDED 2026-08-02**: covered by `tests/scheduler/test_registration.py` (see 4.4's note). `tests/discovery/test_runs.py` has since grown to 7 test executions, adding the digest-link-hierarchy and `accumulated_count` assertions through the real `send_and_advance` path.
- [x] 4.12 Extend `tests/test_architecture.py`: add the `discovery/**` half of rule 4 (may import `sources.contracts`/`notifier.contracts` but never `sources.manganato`/`notifier.telegram`). Done in unit 1 (see 1.16 note); still passes unmodified with `discovery/runs.py` added (imports `notifier.contracts`, which the rule explicitly allows, and no confined third-party module — confirmed `sqlite3` was removed from `runs.py`'s import list specifically to keep the confinement rule green).

## Phase 5: Telegram digest emitter (Unit 5, input contract independent — buildable beside 4)

*Stands alone: `notifier/**` has no DB/source knowledge, tested against the `DigestLine` contract only.*

- [x] 5.1 Verify Unverified claim #5: confirm Telegram's current 4096-char limit and whether `disable_web_page_preview` or `link_preview_options` is the accepted preview-suppression parameter at implementation time. **Both answered, and the answers are recorded at the point of use**: `notifier/telegram.py:18` reads `MESSAGE_LIMIT = 4096  # verified live (task 5.1): 1-4096 chars after entity parsing`, and the parameter chosen is `link_preview_options` (`{"is_disabled": True}`, `telegram.py:217`), not the older `disable_web_page_preview`.
- [x] 5.2 Create `manga_tracker/notifier/telegram.py`: plain HTTPS `sendMessage` via `urllib.request`; confine `urllib.request` to this file only; startup token/chat-id validation fails fast before any send. **Done**: `_call_telegram_api` (`telegram.py:152-161`) is the single call site — `urllib.request.Request` + `urlopen`, POSTing JSON to `{TELEGRAM_API_BASE}/bot{token}/sendMessage` with a 30 s timeout, and catching `HTTPError` to read Telegram's error body rather than raising. Confinement is enforced executably by `tests/test_architecture.py::test_third_party_confinement` (`CONFINEMENT_RULES["urllib.request"] = "notifier/telegram.py"`). The fail-fast gate is `config.require_telegram`, called by `_cmd_run`, `_cmd_run_job` and `_cmd_test_telegram` **before** any `TelegramSender` is constructed; asserted by `tests/test_cli.py::test_test_telegram_fails_fast_when_credentials_are_missing` (both var names in one `SystemExit`, no network reached).
- [x] 5.3 In `telegram.py`, implement digest formatting: HTML (not Markdown), one manga per line blank-line separated, alphabetical by title, chapter numbers shown as-is (decimals included), null progress omits "vas por el" and links to newest chapter, overlong titles truncated with ellipsis, `html.escape()` every interpolated field. **Done, all seven**: `parse_mode: "HTML"` in `_request`; `_format_line` emits `<b>{title}</b> — Cap {n} salió{progress} → <a href="{url}">abrir Cap {n}</a>`; `send_digest` does `sorted(lines, key=lambda line: line.manga_title)`; `_split_message` joins parts with `\n\n`; `_format_chapter_num` keeps `145.5` verbatim and strips the trailing `.0` from a whole number; a null `last_chapter_read` skips the whole progress clause (and `discovery/links.py` step 3 supplies the newest chapter's URL in that case); `_truncate_title` cuts at `TITLE_MAX_LENGTH = 60` with an ASCII `...`, never U+2026; `html.escape` wraps title, URL (`quote=True`) and slug. Covered by `tests/notifier/test_telegram.py::test_formatting_rules_in_one_digest` and `::test_title_is_html_escaped`.
- [x] 5.4 In `telegram.py`, disable link previews on every send (missing from rev 1 and from the `telegram-digest` spec artifact — now restored per design). **Done**: `TelegramSender._request` puts `"link_preview_options": {"is_disabled": True}` in the payload, so it applies to *every* outbound message — digest, heartbeat, dead-slug notice and `test-telegram` alike, since all four funnel through `_send_one` → `_request`. Asserted for every part of a multi-part send by `tests/notifier/test_telegram.py::test_split_all_or_nothing_and_previews_disabled` (`all(call["link_preview_options"] == {"is_disabled": True} for call in calls)`).
- [x] 5.5 In `telegram.py`, implement all-or-nothing size split: exceeding the char limit splits into multiple messages without cutting a manga's line; success only if ALL parts send. **Done**: `_split_message(header, body_lines, limit)` accumulates whole entries and opens a new part rather than cutting one; `send_digest` returns `all([self._send_one(part) for part in parts])` — the list comprehension is deliberate so every part is attempted while the send as a whole is judged. The same splitter is reused by `send_dead_slug_notice`. Asserted by `::test_split_all_or_nothing_and_previews_disabled`, which derives the part count instead of hardcoding it and checks no part exceeds the limit and no line is duplicated or dropped.
- [x] 5.6 In `telegram.py`, implement send retry/rate-limit handling: 429 → wait `retry_after`, retry once; other failure → retry once after a brief wait, then report failure to discovery; never log the request URL or raw response (bot token is in the URL path). **Done**: `_send_one` reads `response["parameters"]["retry_after"]` on failure, falling back to `RETRY_WAIT_SECONDS = 5` when absent, sleeps via the injected `sleeper`, retries exactly once, and returns the second result as a bool. The two log statements emit only `error_code` — never the URL, never the body — and `_call_telegram_api` builds the token-bearing URL as a local. Asserted by `::test_rate_limit_retry_after_is_honored_then_retries_once` (waits `== [17]`) and `::test_second_failure_after_retry_reports_failure` (exactly 2 calls, returns False).
- [x] 5.7 Add `test-telegram` manual verification-message mode to `cli.py`; must not run automatically. **Closed in attempt 14 (unit 7, bring-up tail)**: `_cmd_test_telegram` calls the pre-existing `TelegramSender.send_test_message` via the pre-existing `config.require_telegram()` fail-fast gate; a `test-telegram` subparser registers it. Verified live: fails fast naming both missing env vars, with no network reached, when unconfigured; a CLI-level test with an injected `TelegramSender` proves it reaches the transport when configured. See Phase 7 below and `sdd/v1a-heart-phase/apply-progress` for full detail.
- [x] 5.8 Write `tests/notifier/test_telegram.py`: HTML escaping, alphabetical order, blank-line separation, decimals verbatim, null-progress variant, long-title truncation with ellipsis, link previews disabled on every send, split never cuts a manga line, all-or-nothing success, 429 `retry_after` then one retry, no message when zero novelties. **Done — 11 tests, network-free (the HTTP call is injected as `FakeApi`)**: `test_formatting_rules_in_one_digest` (order, blank line, decimals, whole-number, null progress, truncation in one digest), `test_title_is_html_escaped`, `test_split_all_or_nothing_and_previews_disabled`, `test_rate_limit_retry_after_is_honored_then_retries_once`, `test_second_failure_after_retry_reports_failure`, plus five added later: the accumulation clause on/off, null-progress suppressing both clauses, the local-time header, the Spanish-copy regression, locale-independent month names, the UTC fallback, and `send_test_message`. **One scenario reallocated, not skipped** (same pattern as 4.9): "no message when zero novelties" is decided one layer up — `tests/discovery/test_runs.py::test_zero_candidates_sends_nothing` asserts the sender is never called. `TelegramSender.send_digest`'s own `if not lines: return True` guard (`telegram.py:178`) is therefore correct but has no direct assertion in this file; a one-line test would close that gap if wanted.
- [x] 5.9 Extend `tests/test_architecture.py`: add rule 2 (`notifier/**` imports nothing from storage/sources/discovery/seed) and the `urllib.request`-confined-to-`notifier/telegram.py` half of rule 5. Done in unit 1 (see 1.16 note).

## Phase 6: Scheduler + Docker (Unit 6, deployment only)

*Stands alone: removes deployment wiring only, no application code touched.*

- [x] 6.1 Verify Unverified claim #4: read `EVENT_JOB_ERROR`/`EVENT_JOB_MISSED`/max-instances event constant names off the installed `apscheduler.events`. **Done 2026-08-04 — all 3 constants, read off the installed package.** APScheduler **3.11.3** exports 19 `EVENT_*` names; the three this claim needed are `EVENT_JOB_ERROR`, `EVENT_JOB_MISSED` and — the one the task left unnamed — **`EVENT_JOB_MAX_INSTANCES`**. Nothing had to be corrected: all three exist, so none was invented and none was narrowed away. **Two listeners added** rather than narrowing the claim, because a missed run is the one failure `job_runs` structurally cannot record: a run that fails closes its row `error`, while a run that never started leaves no row at all, and for `active_sweep` — the sole guarantor of the ~24h latency — that absence *is* the failure. `_on_job_missed` logs at ERROR naming the job and the `scheduled_run_time` (which window was lost); `_on_job_max_instances` logs the same class of non-execution from the opposite cause, reading `scheduled_run_times` (plural — `JobSubmissionEvent`, not `JobExecutionEvent`, and the mutation pass proves that attribute name). Both follow `_on_job_error`'s pattern and are registered in `build_scheduler`. Tested by `tests/scheduler/test_registration.py` (3 new tests) which dispatch real `JobExecutionEvent`/`JobSubmissionEvent` objects through the constructed scheduler's own `_dispatch_event`, so the **event mask is under test too** — not just the function body — and still without ever calling `.start()`. The third test asserts the error backstop keeps its own distinct wording, so the two listeners cannot collapse into one indistinguishable line. Proven by an 8-mutation pass (each listener unregistered; each registered on the *other* constant; the miss demoted to `info`; the scheduled time dropped from the message; the singular attribute read on a submission event; the backstop swapped for the miss listener) — **all 8 killed.** Superseded note kept for history: **was partial, verified 2026-08-02 — 1 of 3 constants.** Confirmed: `EVENT_JOB_ERROR`, imported at `scheduler.py:11` and wired at `scheduler.py:149` (`scheduler.add_listener(_on_job_error, EVENT_JOB_ERROR)`); the import resolving is itself the proof of the name, and it is exercised every time `tests/scheduler/test_registration.py` imports the module. Missing: `EVENT_JOB_MISSED` and the max-instances event constant appear **nowhere in the repository** — never read off the installed package, never wired. The shipped listener handles job errors only, which is a defensible narrowing (a missed run is already visible as a gap in `job_runs`), but the claim as written is not closed. Either read the two remaining names off `apscheduler.events` and record them, or narrow the claim on purpose to the one constant in use. No production code depends on the answer today.
- [x] 6.2 Create `manga_tracker/scheduler.py`: `BlockingScheduler`, in-memory jobstore, default executor explicitly `ThreadPoolExecutor(max_workers=1)`; confine `apscheduler` import to this file only. **Done**: `build_scheduler` constructs `BlockingScheduler(timezone=tz, executors={"default": ThreadPoolExecutor(max_workers=1)})` (`scheduler.py:129-131`), with the docstring recording that APScheduler 3.x's own default is 10 so the 1 is a configuration fact, not an inherited default. Confinement of `apscheduler` to this one file is enforced by `tests/test_architecture.py::test_third_party_confinement`. **One nuance**: the in-memory jobstore is APScheduler's default `MemoryJobStore`, left implicit rather than declared. Its consequence — a restart forgets a missed window — is handled deliberately by `sweep_is_overdue`/`catch_up_sweep_if_overdue` reading `job_runs`, not by adopting a persistent store.
- [x] 6.3 Register `feed_check` (`IntervalTrigger(hours=1)`) and `active_sweep` (`CronTrigger(hour, minute)` in local tz, `ACTIVE_SWEEP_HOUR=3` default); both `max_instances=1`; misfire grace 300s (feed) / 3600s (sweep); no run at process start. **Done**: `build_scheduler` registers `feed_check` on `IntervalTrigger(hours=1, timezone=tz)` and `active_sweep` on `CronTrigger(hour=active_sweep_hour, minute=0, timezone=tz)`; `ACTIVE_SWEEP_HOUR` defaults to `3` at `config.py:30`; both carry `max_instances=1` and `misfire_grace_time` of `FEED_GRACE_SECONDS = 300` / `SWEEP_GRACE_SECONDS = 3600`. The tz is passed to each **trigger** as well as to the scheduler, because a trigger constructed before being handed in keeps the tz it resolved through tzlocal — setting it on the scheduler alone looked correct and did nothing (commit `a6d92d4`; production ran the 03:00 sweep at 23:00 local for a while). **Two later changes this task's wording predates**: (a) a third job, `heartbeat`, on `CronTrigger(day_of_week="sun", hour=heartbeat_hour, minute=0, timezone=tz)` with its own `HEARTBEAT_GRACE_SECONDS = 3600`; (b) **"no run at process start" no longer holds literally** — `cli.py::_cmd_run` calls `reap_stale_runs` then `catch_up_sweep_if_overdue` before `.start()`, running one sweep when the last real one is older than a day (one-pager v1.8). `scheduler.py::catch_up_sweep_if_overdue`'s docstring argues why this is not the startup message BOT forbids. The scheduler itself still fires nothing at start.
- [x] 6.4 Wrap each job function so an unhandled exception is caught, closes `job_runs` as `error` with `error_summary`, `logger.exception`; add a scheduler-level error listener as backstop. **Done**: `scheduler.py::_make_job` wraps every job body — one connection opened on the worker thread, `except Exception` → `logger.exception("%s failed", job_name)` → `_close_if_still_open` (sets `finished_at`, `status='error'`, `error_summary` truncated to 200 chars) → `finally: conn.close()`, so nothing escapes to APScheduler (which swallows job exceptions into `EVENT_JOB_ERROR` — the 2025 attempt's cause of death, named in the docstring). The backstop is `_on_job_error` registered via `add_listener(..., EVENT_JOB_ERROR)` at `scheduler.py:149`. Asserted by `tests/scheduler/test_registration.py::test_job_wrapper_closes_the_run_row_and_logs_when_the_job_body_raises` — the wrapper must not raise, `status='error'`, `finished_at` non-NULL, and the log line present.
- [x] 6.5 Add `run` and `run-job {feed_check,active_sweep}` subcommands to `cli.py` invoking `scheduler.py`. **Done**: both subparsers are in `cli.py::build_parser` — `run` → `_cmd_run` (bootstrap, `require_telegram`, `reap_stale_runs`, `catch_up_sweep_if_overdue`, then `build_scheduler(...).start()`), and `run-job` → `_cmd_run_job` → `scheduler.run_job_once`. The `job` choices are now **three**, not two: `feed_check`, `active_sweep` and `heartbeat` (added with the heartbeat job). `test-telegram` was closed separately — see 5.7/7.1.
- [x] 6.6 Write `tests/scheduler/test_registration.py`: asserts trigger types, `max_instances`, misfire values, and `max_workers=1` explicitly present — without starting the scheduler. **Done — 12 tests, none of which calls `.start()`.** The four this task names: `test_both_jobs_registered_with_expected_trigger_types_and_grace` (trigger classes, both `max_instances`, both misfire values) and `test_executor_max_workers_is_explicit_not_the_apscheduler_default` (`_executors["default"]._pool._max_workers == 1`). Notable extra: `test_cron_hours_are_local_hours_not_utc` pins the trigger timezone using `Asia/Tokyo` specifically so the developer machine's tzlocal cannot make it pass by accident — the first version of that test used `America/Caracas` and passed against a broken implementation.
- [x] 6.7 Create `Dockerfile`: `python:3.12-slim-bookworm`, multi-stage (uv-pinned build → runtime copies `/app/.venv`), `uv sync --frozen --no-dev`, OS `tzdata`, non-root fixed UID, `PYTHONUNBUFFERED=1`, no HEALTHCHECK. **Done — every element present**: `FROM python:3.12-slim-bookworm` for both stages; `COPY --from=ghcr.io/astral-sh/uv:0.12.0` (pinned, not `latest`); `RUN uv sync --frozen --no-dev`; runtime stage copies only `/app/.venv`, so neither uv nor pytest ships; `apt-get install --no-install-recommends tzdata`; `useradd --uid 10001` + `USER appuser`; `ENV PYTHONUNBUFFERED=1`; and an explicit comment recording the deliberate absence of `HEALTHCHECK` (no port to probe; the weekly heartbeat is the liveness signal).
- [x] 6.8 At first image build, verify `cffi` resolves to a prebuilt wheel, not an sdist (inspect the build log / `pip show cffi` inside the image). **Still open — the only Phase-6 verification with no evidence anywhere in the repo, searched 2026-08-02.** `docs/runbook-deploy.md` §4 prescribes three post-build image checks (`tzdata OK`, the `appuser` identity, `import pytest` failing) but **not** this one. No build log, no `pip show cffi` output, and no mention of wheel-vs-sdist for this image exists in `docs/`, the `Dockerfile`, or either runbook. The nearest thing, `docs/one-pager-v1a.md`, only argues that `cffi` is the image's single compiled extension and that musl wheels were confirmed to exist — an argument about Alpine, not a measurement of this build. Closes with one `docker compose run --rm --entrypoint sh manga-tracker -c "pip show cffi"` (or re-reading the first build log). Low risk in practice — a four-minute build on `slim-bookworm` succeeded, which an sdist compile would likely not have — but unmeasured. **CERRADO 2026-08-05** contra la imagen desplegada, no por inferencia: `cffi-2.1.0.dist-info/WHEEL` existe con `Tag: cp312-cp312-manylinux_2_17_x86_64` y `_cffi_backend.cpython-312-x86_64-linux-gnu.so` está presente. Ese archivo solo lo escribe una instalación desde wheel, y el tag coincide con `python:3.12-slim-bookworm`. Comando: `docker exec manga-tracker sh -c 'cat /app/.venv/lib/python3.12/site-packages/cffi-*.dist-info/WHEEL'`.
  **Re-checked 2026-08-04, still open, and deliberately not ticked.** No Docker daemon is reachable from this environment (`docker version` fails at `npipe:////./pipe/dockerDesktopLinuxEngine`), so the image cannot be inspected at all: no `pip show cffi`, no dist-info in the copied venv, no build log. What *was* read is `uv.lock`, and it strengthens the inference without settling it — `cffi 2.1.0` is listed with **both** an sdist and 30+ wheels, among them `cffi-2.1.0-cp312-cp312-manylinux2014_x86_64.manylinux_2_17_x86_64.whl`, which matches the runtime exactly (`python:3.12-slim-bookworm` is cp312 on glibc 2.36 ≫ 2.17). So a compatible wheel demonstrably exists in the resolution uv is pinned to; what remains unmeasured is which of the two uv actually installed inside the image. **That is inference, not evidence, so the box stays unchecked.** One command settles it, run on the host that has the image: `docker compose run --rm --entrypoint sh manga-tracker -c "pip show cffi"` — a `Location:` under `/app/.venv` proves installation, and the absence of any `gcc`/`Building wheel for cffi` line in the first build log is the wheel half. Cheaper still if the build log survives: grep it for `Building wheel for cffi` (present ⇒ sdist) versus `Downloading cffi-2.1.0-cp312-cp312-manylinux`.
- [x] 6.9 Verify Unverified claim #1: `docker run --rm python:3.12-slim-bookworm python -c "import zoneinfo; zoneinfo.ZoneInfo('America/Caracas')"` confirms the tz database is present once the `tzdata` OS package is installed. **Done, against the built image rather than the bare base.** `Dockerfile:18` installs OS `tzdata`; `docs/runbook-deploy.md:122-126` records the check as a bring-up step with its expected output — `docker compose run --rm --entrypoint python manga-tracker -c "from zoneinfo import ZoneInfo; ZoneInfo('America/Caracas'); print('tzdata OK')"` → `tzdata OK` — in a runbook whose own header states it was written after the first real deployment. Production corroborates it independently: `scheduler.py::_scheduler_timezone`'s bug note records that on the live server `LOCAL_TIMEZONE` "reached the message formatter and nothing else", which only happens when `ZoneInfo("America/Caracas")` resolves **inside the container** — otherwise `_format_local` would have degraded to its marked `UTC (sin datos de zona horaria)` fallback and the digests would have said so. **Not measured**: the counterfactual half of the claim, i.e. whether the bare `python:3.12-slim-bookworm` would fail *without* the `tzdata` package. The install therefore stays load-bearing by design rather than by demonstration.
- [x] 6.10 Create `docker-compose.yml`: mounts `data/` as a volume, log-driver rotation configured, deploy note documenting the post-restart `run-job active_sweep` mitigation (~47h worst-case latency after an off-window restart). **Done, with the deploy note's content overtaken by a later design change.** Present: the volume `${DATA_DIR:-../manga-tracker-data}:/app/data` (pointed *outside* the repo on purpose — `git clean -xdf` deletes ignored files and `reading_history` is unreconstructible); log rotation `json-file` / `max-size: 10m` / `max-file: 3`; and the deploy note. **The note no longer documents a manual mitigation, because there isn't one**: `run` now reads `job_runs` at startup and sweeps when the last successful one is older than a day, so `run-job active_sweep` is only the on-demand override. Also present beyond this task's wording: `container_name: manga-tracker`, `restart: unless-stopped`, `env_file: .env`, and a `DB_PATH` override that deliberately differs from the `.env` value (host CLI vs container paths reaching one physical file).
- [x] 6.11 Document the non-root UID's one-time `chown` of `./data` (SQLite needs directory write permission in every journal mode, not just WAL). **Done**: `docs/runbook-deploy.md` §2 "Permisos — solo en Linux" gives `mkdir -p ~/manga-tracker-data` + `sudo chown -R 10001:10001 ~/manga-tracker-data`, flags it as "el fallo más probable del primer arranque", and spells out the Docker Desktop exception. It recurs as a row in the first-boot failure table ("El contenedor muere al arrancar, en Linux" / "Falta `chown 10001:10001`") and again under `usermod: group '10001' does not exist`. The *reason* — SQLite needs write permission on the directory in every journal mode, not just WAL — is recorded as a comment at `Dockerfile:21-23`.
- [x] 6.12 Extend `tests/test_architecture.py`: add the `apscheduler`-confined-to-`scheduler.py` half of rule 5 — all 5 AST rules now complete and enforced. Done in unit 1 (see 1.16 note).
- [x] 6.13 End-to-end bring-up per design rollout: `seed` → `test-telegram` → `run-job feed_check` → `run-job active_sweep` → `run`; confirms OP success criteria 1-3. **Done — the sequence ran on a real server, and the documents carry its residue.** `docs/runbook-deploy.md` v1.1 (2026-07-30) opens "Escrito tras el primer despliegue real; cada trampa listada aquí costó tiempo de verdad", and its §5 lists the order actually executed: `test-telegram` → `seed --dry-run` → `seed` → `run-job active_sweep` → `run-job heartbeat` → `up -d`. It records findings only a live run produces — the dry run caught two bad CSV rows (a title with a comma that split the row into five columns, and one missing its first letter), and a load was once interrupted with Ctrl+C. Production-observed defects were then fixed and documented at the site of each fix: cron hours were UTC not local (`scheduler.py::_scheduler_timezone`, commit `a6d92d4`); a killed process left a `job_runs` row open forever and silently disabled `active_sweep` (`scheduler.py::reap_stale_runs`, "Reproduced during the first deploy", commit `40c418e`); a sweep over an empty DB satisfied the catch-up window for a day (`sweep_is_overdue`'s `items_checked > 0` clause, `tests/scheduler/test_registration.py:222-229`, "Observed on the first server"); and three real digests went out in English (commit `0ddac55`). **One deviation from this task's literal sequence**: `run-job feed_check` is not among the runbook's steps — the runbook exercises `active_sweep` and `heartbeat` on demand and leaves the hourly feed to the scheduler, which is consistent with `feed_check` being the opportunistic mechanism rather than the guarantee.

## Phase 7: Bring-up tail (Unit 7, attempt 14 — closes three named gaps blocking the milestone)

*Cuts across units 3/4/5: no new module, all changes land in already-existing files.*

> **Attempt 14 status**: all three scope items closed in one attempt — 184 authored insertions (git diff --stat, additions only) across 11 files, under the 200-line ceiling. Full suite: 78 passed (70 baseline + 8 new tests). `test_architecture.py`: still 4/4, unmodified — `notifier/telegram.py` imports only stdlib (`datetime`, `zoneinfo`) plus its own `contracts`; the accumulated count is computed entirely in `discovery/runs.py` and arrives on `DigestLine` already computed.

- [x] 7.1 Wire `test-telegram` into `cli.py` (same task as 5.7/6.5 above — closed here after two prior deferrals in attempts 12 and 13).
- [x] 7.2 Add `DigestLine.accumulated_count: int = 1` (`notifier/contracts.py`); implement `discovery/runs.py::_accumulated_count(conn, candidate)` (chapter_history rows with `chapter_num > last_chapter_read AND chapter_num <= chapter_num`, capped at `1` when `last_chapter_read is None`); wire into `send_and_advance`'s `DigestLine` construction; render the clause in `notifier/telegram.py::_format_line` only when `accumulated_count > 1`, matching BOT's "vas por el 144, acumulas 2" example verbatim. Closes the gap the Phase 5 notes (Engram artifact) previously flagged as unowned.
- [x] 7.3 Add the local-time header: `DigestSender.send_digest`/`TelegramSender.send_digest` gain a required `now: str` keyword; `TelegramSender` gains `timezone_name` (default `America/Caracas`); `notifier/telegram.py::_format_header` converts via stdlib `zoneinfo.ZoneInfo` and degrades to a marked UTC stamp instead of raising on `ZoneInfoNotFoundError`; `AppConfig.timezone_name` (env `LOCAL_TIMEZONE`, default `America/Caracas`) threads the configured value from `cli.py` into every `TelegramSender` construction site. ~~**`.env.example` documentation deferred**: that exact path was denied by a blanket `.env*` sandbox permission rule this attempt~~ — **CORRECTED 2026-08-02**: `.env.example` *does* document `LOCAL_TIMEZONE=America/Caracas`, with a comment explaining the UTC→local conversion and the fallback. Two loose ends remain, both outside this item's checkbox: (a) `HEARTBEAT_HOUR` is still absent from `.env.example`, while `docs/runbook-deploy.md` §1 instructs the operator to fill "las seis variables" and lists it — copying the example yields five; (b) the comment at `config.py:38-40` still claims neither var is documented there, which is now half false.

## Phase 8: Closing the last heart-phase gaps (attempt 15 — 1.12, 3.11, 6.1 closed; 6.8 left open)

*No new module. Three of the six open items closed; 6.8 stays open for want of a Docker daemon, 1.11 and 2.6 untouched (out of this attempt's scope).*

> **Attempt 15 status**: suite **330 → 347** (17 new tests: 1 client, 1 sweep, 3 scheduler, 12 seed). Two commits. `tests/conftest.py`'s socket block is intact and nothing here touches a network or sleeps. `test_architecture.py`: unchanged and still green — the new `find_slug_owner` lives in `storage/repositories.py` where `sqlite3` is already confined, so the loader gained a validation without gaining a line of SQL, and the new `apscheduler.events` names stay inside `scheduler.py`.
>
> **Concurrency hazard observed, and worth knowing about**: another session was working in this same working tree at the same time. It created `docs/correct-source-audit`, staged five docs, and committed — while this attempt's changes sat uncommitted in the shared tree, which moved the checkout off this branch and carried them along. Nothing was lost (the files were copied out and compared byte-for-byte after switching back), but one file, `docs/spec-importador-kitsu.md`, had both sessions editing **the same line 3**; the other session's version of that line now lives only on its own branch, and this branch carries only the `spec-seed-manual.md` v2.3 → v2.4 pin bump. Whoever merges the two will see a one-line conflict there. Two agents sharing one working tree cannot both hold uncommitted work safely.

### Mutation pass: 36 injected, 36 killed, 0 survivors

Every guard added or relied on by this attempt, broken one at a time against the targeted suite and reverted. The two scheduler listeners are asserted by *distinct* message text precisely because they fail alike — a shared line would hide which non-execution happened, so M08/M09/M14 each kill two tests.

| # | Injected | Killed by |
|---|---|---|
| M01 | D14's list check made a truthiness check (`not data.get("chapters")`) | `test_fetch_chapters_empty_array_is_a_success_not_an_error` |
| M02 | empty array folded into the false-success guard, so it raises `NotFound` | same, + `test_fetch_chapters_missing_array_is_unexpected` |
| M03 | a synthetic chapter substituted for the empty list | `test_fetch_chapters_empty_array_is_a_success_not_an_error` |
| M04 | `consecutive_failures = 0` moved *after* the empty-array check | `test_an_empty_chapters_array_is_a_success_that_resets_the_counter` |
| M05 | empty array counted as a failure (`+ 1`) | same |
| M06 | `if not chapters` weakened to `if chapters is None` | same |
| M07 | `EVENT_JOB_MISSED` listener not registered | `test_a_missed_run_is_logged_loudly_instead_of_vanishing` |
| M08 | miss listener registered on `EVENT_JOB_ERROR` instead | same, + `test_an_erroring_job_keeps_its_own_distinct_backstop_line` |
| M09 | miss logged at `info` instead of `error` | same two |
| M10 | `scheduled_run_time` dropped from the miss message | `test_a_missed_run_is_logged_loudly_instead_of_vanishing` |
| M11 | `EVENT_JOB_MAX_INSTANCES` listener not registered | `test_a_run_refused_for_overlapping_is_logged_too` |
| M12 | max-instances listener registered on `EVENT_JOB_MISSED` instead | same |
| M13 | singular `scheduled_run_time` read off a `JobSubmissionEvent` | same |
| M14 | error backstop swapped for the miss listener | `test_an_erroring_job_keeps_its_own_distinct_backstop_line` |
| M15 | repeated slug requires three rows (`> 2`) | `test_a_slug_repeated_in_the_file_blocks_the_load`, `test_the_same_row_twice_is_reported_once_as_the_slug_error` |
| M16 | repeated slug demoted from error to warning | same two |
| M17 | the database slug check never fires | `test_a_slug_the_database_gave_to_another_manga_blocks_the_load` |
| M18 | the catalogue-owned-title (`kitsu_id`) exemption dropped | `test_a_catalogue_owned_title_is_not_compared_against_the_typed_one` |
| M19 | the database check inverted, so it blocks the legitimate re-run | 3 tests, incl. `test_the_same_title_on_that_slug_is_a_re_run_and_loads` |
| M20 | the check skips the first row holding the slug | `test_a_slug_the_database_gave_to_another_manga_blocks_the_load` |
| M21 | the chapterless warning ignores the row's status | `test_a_row_that_is_not_reading_and_has_no_chapter_does_not_warn` |
| M22 | row warnings never produced | `test_warnings_are_reported_and_load_anyway` + 1 |
| M23 | repeated title warns even on a single slug | `test_the_same_row_twice_is_reported_once_as_the_slug_error` |
| M24 | warnings block the load like errors | `test_warnings_are_reported_and_load_anyway` + 1 |
| M25 | row-count threshold moved to 29 | `test_more_than_thirty_rows_warns_without_blocking` |
| M26 | row-count comparison made inclusive (`>=`) | same |
| M27 | file-level warnings never printed | same |
| M28 | errors printed without their `ERROR` label | 2 tests |
| M29 | warnings printed without their `WARNING` label | 2 tests |
| M30 | the empty `status` column defaults to `want_to_read` | `test_a_row_that_is_not_reading_and_has_no_chapter_does_not_warn` |
| M31 | errors no longer stop the load | 4 tests, incl. the pre-existing `test_invalid_row_blocks_entire_load_and_writes_nothing` |
| M32 | `find_slug_owner` matches on `ms.url` instead of `ms.source_key` | `test_a_slug_the_database_gave_to_another_manga_blocks_the_load` |
| M33 | `find_slug_owner` always reports no `kitsu_id` | `test_a_catalogue_owned_title_is_not_compared_against_the_typed_one` |
| M34 | `find_slug_owner` always reports nothing found | `test_a_slug_the_database_gave_to_another_manga_blocks_the_load` |
| M35 | `write_seed_backfill` loses `chapter_history`'s `INSERT OR IGNORE` | `test_re_running_the_file_reuses_rows_and_records_only_the_progress_that_moved` |
| M36 | the `reading_history` trigger fires even when progress did not change | same |

- [x] 8.1 Write `tests/fixtures/chapters_empty.json` and the two tests it feeds (D14 in the client and in the sweep) — see 1.12.
- [x] 8.2 Implement SEED "Validacion"'s two missing errors and three missing warnings in `manga_tracker/seed/loader.py`, add `repositories.find_slug_owner`, and version `docs/spec-seed-manual.md` to 2.4 with the "otro manga" decision + both dependents' pins — see 3.11.
- [x] 8.3 Read all three event constants off the installed `apscheduler.events` and wire the two missing listeners — see 6.1.
8.4 was the fourth assignment and produced no checkbox of its own on purpose — it *is* 6.8, and duplicating an open box would double-count one verification. Attempted and blocked: no Docker daemon is reachable here, so the image cannot be inspected. The evidence gathered short of that, and the one command that settles it, are recorded in 6.8 above. **Not ticked on inference.**
