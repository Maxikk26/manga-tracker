# Spec: panel web de V1b

Versión 1.7 — 2026-08-25. Depende de `one-pager-v1a.md` (v1.14), `spec-modelo-de-datos.md` (v1.9) y `decision-arquitectura-v1b.md` (v1.2).

Define el alcance funcional del panel: qué pantallas, qué endpoints, en qué orden se entrega y cuándo está terminado. El **dónde y con qué** ya lo cerró `decision-arquitectura-v1b.md` (mismo repo, React + Vite compilado a estáticos, API Python que los sirve, frontera `web` → `storage`); esta spec no lo rediscute.

## Resumen

| Qué | Decisión | Dónde |
|---|---|---|
| **Objetivo** | Editar el progreso de lectura desde el navegador. `reading_history` está en cero desde el 30 de julio y el "vas por el N" del digest se congela en el valor del seed; cada edición del panel corrige ambos | §El corazón |
| **Framework** | **FastAPI** + uvicorn. Validación Pydantic en los endpoints que escriben la base real; OpenAPI gratis que V1c (extensión) consume tal cual | §Decisiones de plataforma |
| **Autenticación** | **Ninguna**, y es decisión, no olvido: monousuario, LAN de casa, el puerto no se expone fuera | §Decisiones de plataforma |
| **Topología** | El panel corre en su **propio contenedor** (misma imagen, mismo volumen): si el panel se cae, el scheduler no | §Decisiones de plataforma |
| **Entrega en 5 fases** | 1: lista + editar progreso y estado. 2: historial y heatmap. 3: alta y baja de mangas. 4: cuatro piezas — migración 3, `import-scores`, `my_score` en la lista y editable, portadas de terminales —, entregadas en **cuatro PRs encadenados** (portadas de terminales primero, sin tocar el esquema; migración 3 sola; `my_score` de punta a punta; `import-scores` al final). **5: el pase de diseño**, que la v1.6 agrega. Cada fase es una entrega separada; la 1 viajó con la v1.0 de esta spec. El tope del heatmap **salió de la fase 4** en la v1.7 — la v1.6 se lo había asignado por error | §Fases |
| **Estado al 2026-08-25** | **Desplegadas**: la **1** (2026-08-18), las **portadas no terminales de la 4** (2026-08-18), la **3** (2026-08-20) y la **2** (2026-08-21, PR #35). Pendientes: el resto de la **4** y la **5** entera. El orden se saltó dos veces y se registra como preferencia, no como restricción | §Fases |
| **Portadas de terminales** | **Se cachean**, revirtiendo la decisión de la v1.0. No rompe "los terminales no consumen requests, nunca": esa regla es sobre **la fuente**, y estos 66 no tienen slug que preguntarle — su portada sale de `media.kitsu.app`, el único host que no exige `Referer`. Costo: **66 requests a Kitsu, cero a manganato** | §Portadas |
| **El pase de diseño (fase 5)** | El rumbo visual nunca se eligió y el rework de navegación que la fase 2 iba a forzar nunca ocurrió. Deja de vivir en notas sueltas fuera de `docs/` y entra como fase, con su criterio de terminado y su runbook | §Fases |
| **Costo por fase** | Fases 1-2: cero requests a la fuente. Fase 3: **3 requests por alta** (ficha para la vista previa, capítulos y portada al confirmar) — corregido de la v1.1, que databa de antes de la decisión de portadas de §128, y **medido de punta a punta** en el panel de desarrollo contra la fuente real: ~2,6 s por alta completa (ficha 0,74 s + portada de la vista previa 1,14 s + confirmación 0,72 s). Aparte, confirmar una portada **ausente** bajó de 43,9 s a 0,7 s, que es la razón por la que `fetch_cover` es la única operación sin reintento. Fase 4: **medido**, ~180 requests una sola vez para las portadas no terminales (35 del tab "Leyendo" + 145 del resto) y **54 MB** en disco para 163 imágenes; más **66 descargas a Kitsu y cero a manganato** para las portadas de los terminales, y **~38 requests a Kitsu** para `import-scores` (resuelve id de MAL a `kitsu_id`, el mismo mecanismo que `import-kitsu`), también cero a manganato | §Fases, §Portadas, §Portadas de los terminales, §`my_score` |
| **Orden de las pestañas** | "Leyendo" por `last_read_at` y "En pausa" por `status_changed_at`, más recientes primero, desconocidos al final por título. Es propiedad de la pestaña, no del request: se ordena en el cliente | §Orden y fechas en la lista |
| **Esquema** | Dos migraciones, no una: la **2** (`bookmarks.status_changed_at`, ya entregada) y la **3** (`bookmarks.my_score`, fase 4). Nada más toca el esquema; el trigger existente es el mecanismo, no un obstáculo | §El corazón, §Fase 4 |
| **Portadas** | Se guarda la **imagen**, no la URL: los hosts de la fuente responden 403 sin su `Referer`, así que una portada por hotlink se ve rota. Archivos en el volumen, servidos por el propio panel | §Portadas |
| **Pantalla principal** | **Grilla de portadas**, no tabla: el dueño escanea portadas y abre la que le engancha. La insignia de atraso se vuelve una pastilla "+N" en la esquina del póster. **Entregado** (`774cdcf`) | §Pantallas |
| **Destino del clic en la portada** | El **listado de capítulos** del manga, no el último capítulo: leyó hasta el 175 de 800, el último está 625 por delante de donde quedó. Sale de `manga_sites.url`, que ya estaba guardada — **cero requests nuevos** y ninguna URL armada por el panel. Abrir el siguiente sin leer, ya resuelto, es la etapa 2 y sigue pendiente | §Pantallas, §Pendientes |
| **Fuera de V1b** | Comandos interactivos del bot, hiatus, segunda fuente, re-sync con Kitsu, edición de metadata de mangas (título, géneros) | §NO entra |

## Decisiones de plataforma

- **FastAPI**, servido por **uvicorn**, en su **propio contenedor** construido de la misma imagen: el scheduler queda intacto como proceso principal del contenedor actual, y el panel es un segundo servicio del compose (`manga-tracker-panel`) que monta el mismo volumen de datos. Decisión del dueño: **si el panel se cae, el scheduler no**. El costo: un contenedor más en el mini-PC (misma imagen, un solo build) y concurrencia SQLite entre procesos — ambos abren la base con `busy_timeout` obligatorio; las escrituras son cortas y poco frecuentes en los dos lados, y si aparecen `SQLITE_BUSY` reales, WAL es la palanca declarada, no una sorpresa.
- **Puerto**: `PANEL_PORT`, default `8000`, publicado en `docker-compose.yml` hacia la LAN. Décima variable de entorno; como `FEED_CHECK_MINUTES`, en un servidor ya configurado no hace falta escribirla.
- **Sin autenticación**: el panel escucha en la LAN de casa y nada lo publica fuera. El día que un túnel lo exponga, la autenticación entra **antes** que el túnel — pendiente declarado, no deuda oculta.
- **Estáticos**: la API monta `frontend/dist/` en `/`; los endpoints viven bajo `/api/`. Sin CORS porque no hay segundo origen.
- **Testing de UI** (decisión del dueño, 2026-08-17: "siempre hacer testing"): **Vitest + React Testing Library desde la fase 1** — tests de componente que fijan el contrato de cable (booleanos, nulls, el blur vacío), la clase exacta de defecto que la verificación SDD encontró dos veces. Un **smoke E2E con Playwright entra en la fase 2**: levantar el sistema real, editar un progreso desde el navegador y verificar la fila en la base. Cada guardián nuevo se rompe a propósito antes de confiar en él, como manda el runbook.
- **Timestamps**: la API entrega UTC tal como está en la base y el frontend convierte a `America/Caracas` al mostrar — con una excepción dura: las **agregaciones por día calendario** (heatmap) aplican la zona **antes** de agrupar, en el backend, o una lectura de las 23:00 cae en el día equivocado. Es la regla de `spec-modelo-de-datos.md` y aquí es donde por fin muerde.

## La frontera, extendida

`decision-arquitectura-v1b.md` fija: `web` importa `storage`, nunca `sources.manganato` ni `notifier.telegram`. Esta spec agrega una precisión que la fase 3 necesitaba, y que la fase 3 ya entregó:

- `web` **tampoco** llama a la fuente indirectamente por cuenta propia: el alta de un manga valida el slug a través de una capa intermedia, y esa capa es la que toca al cliente. El panel pide "agrega esto", no "descarga esto".
- **Corrección de la v1.3 sobre qué capa es esa.** Hasta la v1.2 este documento decía `catalogue`/`importer`, y eso era un error de nombre: `catalogue` es Kitsu detrás de un contrato que no menciona manganato, así que **no puede validar un slug** — no sabe qué es un slug. La capa que la fase 3 construyó es `manga_tracker/intake/`, que recibe el `SourceClient` inyectado y secuencia los tres requests del alta. `web` recibe un `MangaIntake` inyectado y no importa `sources` en absoluto, ni siquiera `sources.contracts`; la regla direccional entregada le prohíbe además `catalogue`, que es justamente el nombre que este documento traía.
- La regla está en `DIRECTIONAL_RULES` en `tests/test_architecture.py` y se probó **inyectando una violación** antes de confiar en ella, como manda el documento de arquitectura.

## El corazón: editar el progreso

Un solo endpoint de escritura hace V1b útil: `PATCH /api/bookmarks/{id}` con `last_chapter_read` y/o `status`.

**El mecanismo ya existe.** El trigger `reading_history_capture_progress` dispara en UPDATE cuando `last_chapter_read` cambia — fue diseñado para esto. El endpoint hace el UPDATE y el trigger captura el evento. Detalles que la implementación debe respetar:

- **`origin='panel'`**: el CHECK de `reading_history.origin` acepta `panel` desde el esquema v1.0, pero el trigger escribe `'manual'` fijo. El repositorio de escritura corrige la fila recién capturada a `'panel'` dentro de la misma transacción. **Cómo NO se hace**: `last_insert_rowid()` — la primera versión de esta spec afirmó que tras el UPDATE apunta al INSERT del trigger, y es falso: SQLite restaura su valor previo al terminar el programa del trigger, así que apuntaría a una fila ajena (o a nada). El mecanismo correcto, verificado con test: capturar `MAX(id)` de `reading_history` como techo antes del UPDATE y corregir `WHERE id > techo AND manga_id = ?` en la misma transacción de escritura — exacto porque el sistema tiene un solo escritor por transacción — condicionado a un espejo en Python del WHEN del trigger (el valor cambió y no es NULL). No se modifica el trigger: la edición directa en SQLite debe seguir registrando `'manual'`.
- **`progress_is_approx` → 0**: una edición desde el panel es un dato exacto; si el valor venía aproximado del import, deja de serlo.
- **`last_read_at`** se sella con el timestamp de la edición (UTC).
- **`status_changed_at`** se sella solo cuando el estado **cambia de verdad**. Volver a elegir en el desplegable el estado que la fila ya tiene no es una transición, y sellarlo ahí degradaría "pausado desde" hasta significar "última vez que toqué el select". Columna de la migración 2; regla completa en `spec-modelo-de-datos.md`.
- **Correcciones a la baja se aceptan y se registran** — el trigger las captura con `previous_chapter_num` mayor, y el consumidor las trata como corrección, no lectura. Regla existente; el panel no la esquiva.
- **Validaciones**: `status` contra el enum del CHECK; `last_chapter_read >= 0`; el bookmark debe existir. Nada valida contra `latest_chapter_num`: leer por delante de lo detectado es legítimo (el lector puede ir por otra fuente).
- **Estados terminales se editan sin efectos**: pasar a `completed`/`dropped` no borra nada; los barridos simplemente dejan de visitarlos, como ya está especificado.

El digest se corrige solo: "vas por el N" lee `last_chapter_read`, así que la primera edición real desde el navegador arregla la sobreestimación acumulada desde julio.

## Orden y fechas en la lista

Entregado el 2026-08-18, encima de la fase 1. La API devuelve la lista completa ordenada por título, que es el orden correcto para las pestañas que se **hojean**. Dos pestañas no se hojean, se **trabajan**, y cada una tiene una fecha que contesta "¿a cuál toqué de último?":

| Pestaña | Se ordena por | Por qué |
|---|---|---|
| **Leyendo** | `last_read_at`, descendente | El manga que toqué de último es el que más probablemente vuelva a tocar |
| **En pausa** | `status_changed_at`, descendente | "Desde cuándo está pausado" es la única pregunta que se le hace a esa pestaña |
| Las demás | El orden de la API (título) | Se hojean; no hay nada que priorizar |

Tres reglas que la implementación fija:

- **Un null es desconocido, no viejo.** No se pliega en la comparación como un cero, porque eso afirmaría que el manga se leyó (o se pausó) en la época Unix. Los desconocidos se hunden **como grupo** y entre ellos se ordenan por título, que es lo que hace que una pestaña sin ninguna fecha se vea deliberada y no barajada.
- **El orden se calcula en el cliente**, no en SQL: la lista se pide entera y se filtra en el navegador, así que el orden es propiedad de la pestaña y no del request. Vive en `frontend/src/domain/sortBookmarks.ts`.
- **Las fechas se comparan como texto**, y eso solo es válido porque todo escritor del backend emite el mismo formato UTC de ancho fijo (`%Y-%m-%dT%H:%M:%SZ`). Meter un `YYYY-MM-DD HH:MM:SS` estilo SQLite en una de esas columnas rompe el orden en silencio, porque el espacio ordena antes que la `T`.

**Qué se va a ver en producción, y no es un defecto**: hoy `last_read_at` es null en los 18 bookmarks de "Leyendo" —solo los `completed` que trajo Kitsu cargan fecha—, así que la pestaña se ve ordenada por título hasta que el panel se use de verdad. Eso es el fallback funcionando, no el orden fallando.

**La columna "Última lectura" muestra el día calendario, sin hora.** La hora de una lectura no le dice nada a quien está decidiendo qué abrir; es ruido. El tooltip de publicación del enlace al capítulo **sí conserva la hora**, porque ahí la hora es justamente el dato. La conversión a `America/Caracas` se aplica antes de leer el día, nunca después — y el test que lo cubre fija `TZ=America/Caracas` en `frontend/vite.config.ts`, porque `Intl` toma la zona de la máquina cuando no se le da una: sin ese pin la aserción pasaría en esta laptop y fallaría en cualquier otra, dejando sin cubrir exactamente lo único que esos tests existen para atrapar.

## API

Todos los endpoints bajo `/api`, JSON, UTC. Errores como `{"detail": ...}` (el formato de FastAPI).

| Método y ruta | Fase | Qué hace |
|---|---|---|
| `GET /api/bookmarks` | 1 | Lista con título, estado, `last_chapter_read`, `latest_chapter_num`, atraso calculado, `manga_url`, `latest_chapter_url`, `last_read_at` y `status_changed_at`. Ordenada por título; el orden por pestaña lo pone el cliente. Filtro `?status=`. `manga_url` es la columna `manga_sites.url` servida tal cual — es la página del manga, y el panel **no** la deriva recortándole la cola a `latest_chapter_url` (ver §Pantallas) |
| `PATCH /api/bookmarks/{id}` | 1 | Edita progreso y/o estado (ver §El corazón) |
| `GET /api/history/reading` | 2 | `reading_history` agregada por día calendario **local** (`zoneinfo` en Python, nunca funciones de fecha de SQL), ventana móvil de `days` días terminando hoy, default 365. Las correcciones a la baja no suman al heatmap. Desplegado el 2026-08-21 (PR #35) |
| `GET /api/mangas/{id}/history` | 2 | Historial por manga: lecturas y publicaciones (`chapter_history`) entrelazadas cronológicamente; una corrección a la baja sigue visible aquí, con `delta` negativo. `publications_since` marca desde cuándo hay publicaciones detectadas — nunca afirma completitud, porque `CHAPTER_HISTORY_LIMIT` solo topó el backfill único. Desplegado el 2026-08-21, pero **no alcanzable desde la UI** por decisión del dueño (ver §Pendientes abiertos) |
| `POST /api/mangas/preview` | 3 | Vista previa del alta: resuelve el slug y devuelve título, portada candidata y estado de publicación **sin escribir ninguna fila**. Entregado el 2026-08-20 |
| `POST /api/mangas` | 3 | Alta: URL de manganato + estado + capítulo inicial. Valida el slug vía `intake` (no vía `catalogue` — ver §La frontera); crea manga, `manga_site` y bookmark con `origin='manual'` en una sola transacción. Entregado el 2026-08-20 |
| `GET /api/mangas/preview-cover` | 3 | Proxea la portada de la vista previa. No estaba previsto: los hosts de imágenes de la fuente responden 403 al hotlink, así que sin proxy toda vista previa mostraba el placeholder. Entregado el 2026-08-20 |
| `DELETE /api/bookmarks/{id}` | 3 | **No existe.** La baja es `status='dropped'`: borrar destruiría `reading_history`, que es el dato que nunca se recupera. Decisión, no omisión |
| `GET /api/covers/{manga_id}` | 4 | Sirve la portada cacheada desde disco; 404 si no está cacheada. **Nunca** dispara un fetch. Entregado el 2026-08-18 (ver §Portadas) |

## Pantallas

Tres superficies, sobrias — dos pantallas y un modal, tras la corrección de la 3 más abajo. El panel es una herramienta de uso diario de un solo usuario, no un producto.

1. **Lista** (fase 1): los bookmarks con estado, progreso editable inline, atraso visible de un vistazo, link al listado de capítulos del manga. Filtro por estado. Es la pantalla por defecto y la única imprescindible.
2. **Historial** (fase 2): el heatmap de lecturas por día. **Desplegada el 2026-08-21** — se llega por un switch (`AppNav`) desde la lista, sin router. La línea de tiempo por manga se construyó pero **no es alcanzable desde la pantalla** (ver §Pendientes abiertos): el dueño decidió el mismo día que no le interesa verla. El formato del heatmap ya **no** está pendiente — se cerró ese día con `/prototype`: calendario anual completo, una columna por semana, con los días vacíos visibles en vez de colapsados. Lo que sigue detrás de su seam es la escala (`domain/heatmapBuckets.ts`), y lo que sigue sin decidirse es el tono/tamaño de la pastilla "+N", detrás de las custom properties de `.behind-pill`.
3. **Alta** (fase 3): un formulario — URL, estado inicial, capítulo. El resultado del matching se muestra antes de confirmar. **Entregado el 2026-08-20 como modal sobre la lista, no como pantalla propia** (decisión del dueño, 2026-08-19): el alta es una interrupción de la lista, no un destino al que se navega, y el modal cierra al confirmar y refresca la grilla. Con eso el alta no suma una pantalla: hoy hay **una** pantalla y un modal, y la segunda pantalla llega recién con la fase 2.

**Corrección de la v1.1 sobre el papel de la portada.** La v1.0 decía que las portadas "decoran la lista"; el dueño decidió lo contrario el 2026-08-18, y con una razón que no es estética: él **escanea portadas primero y abre la que le engancha**, así que la portada es la puerta de entrada al manga, no su adorno. La pantalla de lista pasa por tanto a ser una **grilla de portadas**, y la tabla deja de ser la forma por defecto.

Va con ella el destino de la insignia de atraso: "Vas atrasado N capítulos" se vuelve una pastilla compacta "+N" en la esquina del póster. La justificación es medible — la insignia se dispara en 18 de las 18 filas de "Leyendo", así que como prosa no transporta ninguna información; lo único que distingue una fila de otra es el número.

**Entregado el 2026-08-18** (`774cdcf`). El póster completo es el enlace: la portada es la entrada, no el adorno, así que el objetivo de clic es la imagen y no una palabra debajo. El título va debajo recortado a dos líneas, como confirmación. Cuando el API no tiene portada cacheada —404, un estado ordinario— la tarjeta cae a las iniciales de las dos palabras significativas sobre un color derivado del título, no a un recuadro gris: en esta lista "Genius" aparece en tres títulos y "Regressed" en dos, y un gris los dejaría igual de indistinguibles que antes. El selector de estado se conserva en la tarjeta; el prototipo no lo traía, y perderlo habría tirado funcionalidad de la fase 1 en un cambio que era solo visual. Llega con **modo oscuro**, que el panel no tenía.

**A dónde lleva el clic: al listado de capítulos, no al último capítulo.** Corrección de la v1.4, decidida por el dueño el 2026-08-21. Hasta esa fecha el póster enlazaba a `latest_chapter_url`, que es el capítulo **más nuevo** — exactamente el que el dueño no quiere abrir. El caso normal de esta lista lo deja claro: leyó hasta el 175 de 800 disponibles, y el último está 625 capítulos por delante de donde quedó. El listado del manga es el destino porque desde ahí él encuentra su lugar a mano.

Esto se entrega en **dos etapas**, y la 1 es la que existe hoy:

| Etapa | Destino del clic | Estado |
|---|---|---|
| 1 | El listado de capítulos del manga (`manga_url`) | **Entregada el 2026-08-21** |
| 2 | El capítulo siguiente sin leer, resuelto | Pendiente, ver §Pendientes abiertos |

**El panel no arma la URL, la sirve.** `manga_sites.url` ya guarda la página del manga desde la primera versión del alta —las tres rutas de inserción la escriben, y en producción las 170 filas mapeadas la tienen, ninguna nula—, así que la etapa 1 no dispara **ningún** request nuevo ni construye ninguna dirección: agrega una columna al `SELECT` del panel. La alternativa que parece más simple —recortarle el último segmento a `latest_chapter_url`, porque `…/manga/x/chapter-33` sin la cola es `…/manga/x`— es la que hay que rechazar: arma una URL de la fuente dentro de un componente de React, que es la frontera estructural que CLAUDE.md señala como la más fácil de violar por accidente. Además las dos direcciones dejan de coincidir en cuanto la fuente renombra un slug, y el test que lo fija hace que discrepen a propósito, porque en producción coinciden y una derivación pasaría inadvertida.

Un efecto lateral buscado: el enlace ahora depende de que exista el mapeo a la fuente, no de que haya habido una detección. Un manga recién dado de alta y todavía sin barrer ya enlaza a su listado, cosa que antes no hacía. Los que siguen sin enlace son los bookmarks sin fila en `manga_sites`, y ahí no hay nada que enlazar. **Corrección de la v1.6 sobre quiénes son.** Este documento los describía como "entradas de Kitsu a las que nunca se les pegó la URL, 59 de 229 en producción", y las dos mitades quedaron cortas: hoy son **66 de 236**, y sobre todo **no son un resto disperso — son exactamente los 66 bookmarks en estado terminal**, medido el 2026-08-25. Ningún bookmark no terminal está sin mapear. El corte cae limpio sobre el estado porque los barridos nunca visitan un terminal y por tanto nunca le aprenden un slug; el importador ya lo había registrado. Consecuencia práctica **hoy**: un `completed` o un `dropped` sin mapeo no tiene enlace a su listado.

**Corrección de la v1.7: esa consecuencia describe cómo llegaron estas 66 filas, no una propiedad de `completed`/`dropped`.** No es que un terminal no *pueda* enlazar — es que ningún terminal de hoy llegó a serlo con un mapeo puesto. El panel edita `bookmarks.status` sin tocar `manga_sites`, así que el primer manga `reading` mapeado que el dueño marque `completed` desde la lista conserva su fila en `manga_sites` y, con ella, su enlace. Esa fila también sería el primer caso real de la fase 4: un terminal **mapeado**, con `cover_url` conocido, elegible para la ruta de portadas de terminales igual que cualquier otro (§Portadas de los terminales) — la mera presencia del mapeo no lo saca de esa ruta ni lo mueve a la ruta mapeada, que sigue reservada a los no terminales.

## Portadas (fase 4) — entregado el 2026-08-18

La frontera manda: el panel **no** descarga portadas. Lo que la v1.0 no sabía es que tampoco puede enlazarlas.

**Guardar la dirección no es guardar la imagen.** Medido el 2026-08-18: los hosts de imágenes de manganato (`img-r2.2xstorage.com`, `storage.waitst.com`) responden **403** a un request que no lleve un `Referer` de manganato, y **200** al que sí. El CDN de Kitsu (`media.kitsu.app`) responde 200 a cualquiera. O sea que un `<img src>` apuntando al `cover_url` guardado se vería roto en **toda** portada tomada de la fuente — un fallo que se despliega en silencio y parece un problema de CSS. Por eso se cachean bytes y no URLs, y por eso el panel sirve los bytes él mismo.

**La medición también cambió el tamaño del trabajo.** 212 de los 229 mangas ya traían `cover_url` del import de Kitsu; los 17 que no, son todos `origin='seed'` y son 17 de las 18 filas de "Leyendo": el hueco no estaba repartido, estaba exactamente donde duele. El costo real se reparte: el tab "Leyendo" costó 35 requests (17 fichas para aprender la URL + 18 imágenes) y 528 KB, y el resto de la población no terminal costó 145 imágenes más — **~180 requests en total y 54 MB para 163 portadas**, del mismo orden que los ~230 que esta spec estimaba en su v1.0. Lo que la medición abarató no fue el número de requests sino su forma: casi ninguno necesita pedir la ficha, porque la URL ya estaba.

**El one-off CLI `cache-covers`.** Recorre los mangas con mapeo a la fuente en los estados no terminales y les deja un archivo en `data/covers/{manga_id}.{ext}`, dentro del volumen persistido, junto al archivo de la base.

| Aspecto | Decisión |
|---|---|
| Población | Estados `reading`, `want_to_read`, `on_hold`, **y solo para esta ruta**, que es la que le pregunta a la fuente. Los terminales entran desde la v1.6 por una ruta distinta que no toca la fuente — §Portadas de los terminales |
| Flags | `--status` (repetible, sustituye la población por defecto), `--limit`, `--dry-run` |
| Costo | A lo sumo **dos** requests por manga, cada uno salteado si ya está hecho: aprender el `cover_url` si falta, y bajar la imagen. Con el delay de 5-15s de siempre, secuencial, sin concurrencia |
| Idempotencia | Una segunda corrida solo cuesta lo que falte. Una corrida interrumpida conserva lo que ya pagó: cada imagen se escribe como `.part` y se renombra al terminar, así que un cuerpo a medias nunca queda registrado como hecho |
| Qué **no** toca | `consecutive_failures`, en ninguna de las dos direcciones. Ese contador alimenta el aviso de slug muerto desde los mecanismos de detección; el mantenimiento no puede tener el poder de pausar un mapeo ni de borrar en silencio una racha real |
| Qué no es | No es un job: no abre fila en `job_runs`, no tiene horario y no manda nada. Es mantenimiento, se corre a mano |

**Dónde vive cada mitad.** Saber qué `Referer` exige el CDN es conocimiento de la fuente, así que `fetch_cover` está en el cliente; decidir qué mangas merecen un request necesita la lista de lectura, así que eso está en `discovery/covers.py`. Ninguna de las dos aprende la mitad de la otra. La **ubicación** del cache es de `storage/cover_cache.py`, no de discovery: servirlo desde `web` habría significado `web → discovery`, que el panel no tiene por qué importar — es el módulo que hace requests a la fuente. `storage` es el dueño honesto, porque el cache es dato persistido al lado de la base, en el mismo volumen, y sobrevive a un rebuild por la misma razón que ella.

**El endpoint.** `GET /api/covers/{manga_id}` devuelve el archivo cacheado y **404 cuando no hay**. Ese 404 es un estado ordinario, no un fallo: un manga puede estar listado mucho antes de que `cache-covers` lo alcance, así que el frontend lleva fallback y no asume que esto siempre contesta. La respuesta va con `Cache-Control: public, max-age=86400` más etag y last-modified: una portada solo cambia cuando alguien la recachea a propósito, así que un día de frescura baja las ~18 peticiones por visita a cero sin dejarla clavada — un archivo cambiado se recoge en la próxima revalidación.

**El media type sale de una tabla propia**, no de `mimetypes`. `mimetypes` lee el registro del sistema, donde `.webp` suele faltar en Windows, y ahí `FileResponse` adivina `application/octet-stream`. Como casi toda portada tomada de la fuente es `.webp`, el tipo servido habría dependido de **qué máquina contestó** —correcto en el contenedor, equivocado en una laptop— sin error en ninguno de los dos lados.

**Mangas nuevos (fase 3)**: el alta ya visita la ficha para validar; la misma operación guarda la portada al pasar. Sin job periódico: las portadas no caducan a esta escala, y el one-off se puede relanzar a mano.

## Portadas de los terminales (fase 4)

**La v1.0 los dejó fuera y la v1.6 los mete.** El texto que se revierte decía que "una portada no es la excepción que rompe esa regla", y era una lectura razonable de una regla que igual no está en juego. Lo que la cambió fue medir, el 2026-08-25, contra la base de producción.

| Medición | Resultado |
|---|---|
| Bookmarks en estado terminal | **66** — 28 `completed` y 38 `dropped` |
| Cuántos ya tienen `cover_url` | **Los 66.** Ninguno falta |
| De qué host es esa URL | **`media.kitsu.app` los 66.** Ninguno de manganato |
| Cuántos tienen fila en `manga_sites` | **Cero** |

**Los 66 terminales son exactamente los 66 bookmarks sin mapeo a la fuente.** De 236 bookmarks, 170 están mapeados y 66 no, y el corte cae limpio sobre el estado: ningún bookmark no terminal está sin mapear. No es casualidad ni descuido — es la regla funcionando. Los barridos nunca visitan un terminal, así que nunca le aprendieron un slug, y el importador ya lo había registrado en su momento: los títulos terminales se importan sin mapeo porque esa data solo vive en Kitsu.

**Esto describe cómo llegaron estas 66 filas, no un invariante del estado terminal (corrección de la v1.7).** El panel puede crear un terminal *mapeado* con un solo clic — pasar un `reading` ya mapeado a `completed` no toca `manga_sites` — y por eso la ruta de este apartado no puede decidir por la presencia de un mapeo: decide solo por `bookmarks.status`, y una fila que resulte estar mapeada se sirve igual, por esta ruta, nunca por la de §Portadas. La consulta nueva (`list_stored_url_cover_candidates`) no hace `JOIN manga_sites` — ni para incluir ni para excluir — precisamente para no codificar la coincidencia de hoy.

**Por eso la regla dura no se está rompiendo.** "Los terminales no consumen requests, nunca" es una regla sobre **la fuente**, y aquí no hay fuente a la que preguntarle: sin slug no hay ficha, no hay capítulos y no hay nada que manganato pueda contestar. La portada sale de una URL de Kitsu que el import ya guardó, y `media.kitsu.app` es —según §Portadas, medido el 2026-08-18— el único de los tres hosts que responde 200 sin `Referer`. El costo es **66 descargas a Kitsu y cero requests a manganato**, y esa segunda mitad es estructural, no una promesa: no hay slug con el que hacerlas aunque alguien quisiera.

**La estimación de "~200 requests" que este documento traía describía otra operación.** Contaba visitar la ficha de la fuente para aprender la URL más bajar la imagen, que es lo que cuesta un manga mapeado sin `cover_url`. Para estos 66 la primera mitad no existe.

**Qué hay que construir, y es menos de lo que parece.** `list_cover_candidates` hace `JOIN manga_sites` **INNER**, a propósito y documentado: sin mapeo no hay slug al que preguntarle, y listar esa fila solo produciría trabajo que el llamador tiene que descartar. Así que pasarle `--status completed` hoy devuelve **cero filas**, y agregar el flag no alcanza. Hace falta una segunda ruta que baje un `cover_url` ya conocido sin pasar por el slug — **más corta** que la existente, porque se salta la mitad cara. Lo que no cambia: el mismo `BATCH_POLICY`, el mismo `.part` y renombre, la misma idempotencia, el mismo `GET /api/covers/{manga_id}` sirviéndolas.

**Lo que esto no autoriza.** Ni un request de detección a un terminal, ni volver a mapearlos, ni tocar `consecutive_failures`. La puerta que se abre es exactamente una: bajar una imagen de un CDN público cuya dirección ya estaba guardada.

**Hueco encontrado y dejado sin cerrar a propósito (v1.7).** Las dos rutas de portada se reparten por `bookmarks.status`: la mapeada sirve los no terminales, la de esta sección sirve los terminales — pero ambas asumen, sin decirlo, que un bookmark **no terminal sin mapeo** no existe. Hoy es cierto: de los 66 sin `manga_sites`, los 66 son terminales, así que un no-terminal desmapeado con un `cover_url` ya guardado (por ejemplo, uno traído por Kitsu y vuelto a poner en `reading` sin que nadie le pegara la URL) no es candidato de ninguna de las dos rutas. Población de ese hueco: **cero filas hoy**. No se cierra en esta entrega — ampliar la ruta mapeada a un `LEFT JOIN` es un cambio de comportamiento a un comando ya desplegado, y queda anotado para la fase 5 o para el día en que ese hueco tenga su primera fila real.

## `my_score` (fase 4)

- **Migración 3**: `bookmarks.my_score INTEGER` (escala 0-10 del export; NULL = sin puntuar). `PRAGMA user_version` 2 → 3, con el mecanismo que ya existe y su respaldo previo obligatorio. **Era la migración 2 en la v1.0 de esta spec**, y dejó de serlo: el número 2 lo tomó `status_changed_at` el 2026-08-18, y un número desplegado no se reutiliza porque está grabado en el `user_version` de cada base.
- **Backfill one-off** (`import-scores`): lee el `kitsu-manga.xml` que sigue en `~/manga-tracker-data/`. **Corrección de la v1.7**: el id que trae el archivo (`manga_mangadb_id`) es un id de **MAL**, no el `kitsu_id` que ya vive en `mangas` — hay que resolverlo primero contra el catálogo de Kitsu (`/mappings`, en bloques de 12), el mismo mecanismo que usa `import-kitsu`, antes de poder buscar el bookmark por `kitsu_id`. Costo medido para las 218 entradas del export: **~38 requests a Kitsu** (~19 de resolución más ~19 de categorías, que el cliente del catálogo siempre pide y esta corrida no necesita), cero a manganato. Llena solo columnas en NULL — un score ya puesto a mano no se pisa. Score 0 del export = NULL, no cero.
- El panel lo muestra en la lista y lo edita por el mismo `PATCH`. **A diferencia de `last_chapter_read`, sí se puede volver a NULL**: `{"my_score": null}` borra un score mal tipeado, porque un score no alimenta el trigger ni `reading_history` ni el digest — solo la lista —, así que no hay historia irrecuperable que proteger al permitirlo. Cierra el pendiente declarado en `spec-importador-kitsu.md` desde V1a.

## Fases y criterios de terminado

Cada fase es **una entrega** (rama, PR, deploy) con su criterio verificable. La v1.0 fijó además que nada de la fase N+1 empieza sin la N desplegada; el orden real se saltó esa regla dos veces y la nota bajo la tabla explica por qué eso no costó nada.

| Fase | Contenido | Criterio de terminado |
|---|---|---|
| **1 — corazón** ✅ **entregada el 2026-08-18** | FastAPI + uvicorn en su propio contenedor (`manga-tracker-panel` en el compose, scheduler intacto), `GET/PATCH bookmarks`, frontend con la pantalla de lista, etapa Node en el Dockerfile, regla direccional probada con violación inyectada | Una edición real de progreso hecha desde el navegador del teléfono/laptop aparece en `reading_history` con `origin='panel'`, y el digest siguiente usa el valor nuevo |
| **2 — historial** ✅ **desplegada el 2026-08-21** (PR #35), verificada contra los contenedores corriendo: digest de imagen coincidente, `GET /api/history/reading` en 200 y el bundle servido con el heatmap | Los dos endpoints de lectura y las vistas de heatmap y por-manga, más el smoke E2E con Playwright | El heatmap muestra las lecturas reales acumuladas desde la fase 1, agrupadas en día local correcto (**verificado con una lectura nocturna**: `2026-08-20T03:30:00Z`, las 23:30 del 2026-08-19 en Caracas, cae en el día local `2026-08-19`, nunca en el `2026-08-20`) |
| **3 — alta** ✅ **entregada el 2026-08-20** | `POST /api/mangas/preview` y `POST /api/mangas` vía `intake` + modal de alta sobre la lista | Un manga agregado desde el panel queda con mapeo válido y entra al barrido diario siguiente sin intervención. **Cumplido**: el alta escribe `mangas`, `manga_sites` y `bookmarks` en una transacción, con `status_changed_at` sellado y `origin='manual'`, y un alta sin capítulos publicados deja `latest_chapter_num` en null para que el barrido siguiente la selle |
| **4 — extras** 🟨 **portadas no terminales entregadas el 2026-08-18**; el resto pendiente | **Cuatro piezas**: migración 3, `import-scores`, `my_score` en la lista y editable, portadas de los 66 terminales — entregadas en cuatro PRs encadenados, uno por pieza. El tope del heatmap **ya no es parte de esta fase** (v1.7; ver §Pendientes abiertos) | Scores del export visibles y editables por el mismo `PATCH`; los 66 terminales con portada servida desde disco; `user_version=3` en producción con respaldo previo |
| **5 — el pase de diseño** ⬜ **no empezada** | El rumbo visual, que nunca se eligió; el rework de navegación que la fase 2 iba a forzar; el tono y el tamaño de la pastilla "+N"; la puerta de entrada de la línea de tiempo; la grilla por ancho de pantalla; el logo | Un pase corrido con el protocolo de `runbook-diseno-ui.md` —contendientes, compuertas, puntuación— con su comparación registrada, y el rumbo elegido escrito **aquí**, no en una nota suelta |

**Lo que se adelantó de la fase 4.** `cache-covers`, `GET /api/covers/{manga_id}` y la migración 2 se entregaron el 2026-08-18, antes que las fases 2 y 3, y la regla de "nada de la fase N+1 empieza sin la N desplegada" no se rompió por descuido: la portada dejó de ser un extra cuando se vio que las 18 filas de "Leyendo" son de un mismo género cuyo vocabulario colisiona —"Genius" en tres títulos, "Regressed" en dos—, así que el título solo **no identifica el manga**. Eso la mueve al corazón del problema que el panel resuelve. Lo que queda de la fase 4 —las cuatro piezas de la tabla de arriba: migración 3, `import-scores`, `my_score` y las portadas de los terminales— sigue en su sitio.

**Y la fase 3 se adelantó a la fase 2**, así que el orden entregado fue 1 → portadas de la 4 → 3 → 2. La regla del orden queda entonces registrada como lo que resultó ser: **una preferencia por defecto, no una restricción**. Las dos veces que se saltó fue por la misma razón —lo que el dueño necesitaba a diario no era lo que seguía en la lista— y ninguna de las tres costó nada, porque las fases del panel no se apoyan unas en otras: la 2 solo lee `reading_history`, que la 1 ya llena, y la 3 solo escribe filas nuevas. La fase que sí tiene una dependencia real es la 4, que necesita la migración 3. La fase 2 arrastraba por haberse postergado **una** deuda de tests, no dos: el test de regresión del progreso en estado terminal ya se había cerrado antes de esta entrega (PR #33, `e22309f`, `tests/web/test_panel_api.py:357`), y lo único que seguía abierto era el smoke E2E con Playwright — cerrado ahora con esta entrega.

**Por qué el pase de diseño pasa a ser una fase.** Hasta la v1.5 no estaba en ninguna parte de `docs/`. Vivía en tres pedazos sueltos: una lista de punchos del 2026-08-17 cuyo encabezado era "quitar el AI slop" y que registra que **el rumbo visual nunca se eligió** —había tres opciones preparadas y no se decidió ninguna—; unas observaciones del 2026-08-20 que pedían un pase de diseño para la pastilla "+N" y avisaban que **la fase 2 iba a forzar un rework de navegación**; y `runbook-diseno-ui.md`, que se escribió el 2026-08-21 y dice **cómo** correr un pase de diseño pero no dice que haya que correr uno. Un manual sin nada que lo agende no es un plan. El aviso además se cumplió: la fase 2 aterrizó con Historial, heatmap y un switch de navegación nuevo, y el rework no ocurrió. En este repo lo que no está en `docs/` se pierde, y esto se perdió durante ocho días.

**El orden entre la 4 y la 5, y qué se pisa de verdad.** La fase 4 le agrega cosas a la lista y la fase 5 rehace la lista, así que la pregunta obvia es si la 4 es trabajo que se tira. **La respuesta es no, y la distinción es entre el dato y su forma.** Migración 3, `import-scores` y las portadas de los terminales son esquema, un CLI y archivos en disco: nada de eso lo toca un rediseño. Lo que sí se rehace es **dónde va el score en la tarjeta, de qué tamaño y de qué color**, y esa es precisamente la clase de decisión que la fase 5 existe para tomar. La regla que sale de ahí: **la fase 4 entrega el dato y su edición por el camino más liso posible, y no gasta ni una decisión visual en él.** Ponerlo bonito antes de elegir el rumbo es pintar una pared que se va a tumbar.

**V1b está terminado** cuando las cinco fases están desplegadas y `reading_history` acumula ediciones reales de más de una semana — la señal de que el panel se usa, no solo existe. El reloj de esa semana corre desde el 2026-08-18.

## NO entra en V1b

- Comandos interactivos del bot (`/status`, `/check`): V1b+ según el one-pager; el panel los vuelve menos urgentes.
- Detección de hiatus / fin de publicación: sigue esperando meses de `chapter_history`.
- Segunda fuente (V2), extensión de Firefox (V1c), re-sync hacia Kitsu (sin fecha).
- Edición de metadata de mangas (título, géneros, sinopsis): SQLite directo sigue siendo la vía; el panel edita **bookmarks**, no el catálogo.
- Responsive fino / PWA / dark mode como requisitos: si salen gratis con el stack, bien; no son criterio de nada. **Precisión de la v1.6**: que la fase 5 pueda decidir la grilla por ancho de pantalla no los convierte en requisitos. El pase de diseño puede resolverlos si el rumbo elegido los pide; lo que sigue sin ser criterio de terminado de nada es que existan. El modo oscuro, de hecho, ya llegó gratis el 2026-08-18 y así se registra: como regalo, no como deuda saldada.

## Parámetros de configuración

| Parámetro | Default | Notas |
|---|---|---|
| `PANEL_PORT` | `8000` | Décima variable. En servidores ya configurados no hace falta escribirla |

## Decisiones discutibles

- **FastAPI sobre Flask**: se paga en paquetes (starlette, pydantic, uvicorn) lo que se gana en validación tipada de las escrituras y el contrato OpenAPI para V1c. A esta escala Flask alcanzaba; la validación de lo que escribe la base real inclinó la balanza.
- **Panel en contenedor propio, no en el proceso del scheduler**: la alternativa (uvicorn como proceso principal con el scheduler arrancando en su lifespan) era un contenedor menos y cero concurrencia entre procesos, y se descartó por decisión del dueño: el aislamiento gana — un panel caído o colgado no puede tumbar la detección, que es la parte del sistema que no puede parar. El precio, asumido: dos contenedores de la misma imagen y la concurrencia SQLite descrita en las decisiones de plataforma.
- **Sin DELETE**: hay quien preferiría poder borrar de verdad. La respuesta es que `reading_history` es el único dato irrecuperable del sistema y `dropped` ya expresa "no me interesa más" sin destruirlo.
- **Corregir `origin` tras el trigger en vez de modificar el trigger**: deja una asimetría (el trigger dice `manual` y el repositorio lo corrige), pero modificar el trigger exigiría migración y rompería el registro correcto de las ediciones hechas por SQLite directo.

## Pendientes abiertos

- Autenticación queda explícitamente en "ninguna" **mientras nada exponga el puerto fuera de la LAN**. Exponerlo convierte esto en bloqueante, no en mejora.
- ~~El formato exacto del heatmap (buckets, colores, si quiere semanas o meses) se decide con la pantalla enfrente, no aquí.~~ **Cerrado el 2026-08-21**, el mismo día, con `/prototype`: calendario anual completo, una columna por semana, días vacíos visibles en vez de colapsados. §Pantallas ya lo registraba desde la v1.5 y **este pendiente se quedó contradiciéndolo cuatro días** — el documento se estaba desmintiendo a sí mismo. Lo que sigue abierto de esa nota es solo la mitad que no era el formato: la **escala** detrás de `frontend/src/domain/heatmapBuckets.ts` (sin tests, a propósito) y el **tono y tamaño de la pastilla "+N"** detrás de las custom properties de `.behind-pill` en `styles.css`. Ambos son decisiones visuales y pasan a la **fase 5**; los seams existen justamente para que ese pase reemplace un archivo entero sin tocar un solo test.
- `cadence_days_estimate` sigue sin consumidor; si el panel quiere mostrar "publica cada ~N días", es cálculo de presentación, no de esquema. **Va a la fase 5**: entra si el rumbo visual lo pide, y si no, se queda sin consumidor sin que eso sea deuda.
- ~~**El alta de mangas (fase 3) debe sellar `status_changed_at` al crear el bookmark.**~~ **Cerrado el 2026-08-20.** El formulario existe y sella la columna: `write_manual_add` en `manga_tracker/storage/repositories.py` la incluye en el `INSERT` del bookmark con el mismo `now` que el resto de la transacción, y su docstring registra el porqué — el estado de una fila recién creada acaba de cambiar, por definición, en el momento en que se creó. El pendiente se abrió porque en su momento no había nada que hacer; ya no lo hay porque está hecho.
- **La grilla no está descrita al detalle todavía**: la spec fija la dirección y el comportamiento, no el número de columnas por ancho de pantalla ni qué pasa en teléfono. Hoy es `auto-fill` con un mínimo de 150 px, decidido en el código y no aquí. **Va a la fase 5**, que es donde se dibuja en vez de describirse.
- ~~**Ningún manga en estado terminal tiene portada cacheada, y así se queda.** Si algún día la pantalla de historial quiere mostrar los `completed`, hay que decidir si vale gastar ~200 requests o si ahí se vive con el placeholder.~~ **Cerrado el 2026-08-25 como decisión: se cachean, y entran en la fase 4.** El pendiente presentaba una disyuntiva falsa porque su número era de otra operación. Medido: los 66 ya tienen `cover_url`, los 66 apuntan a `media.kitsu.app` y ninguno tiene mapeo a la fuente, así que el costo real son **66 descargas a Kitsu y cero requests a manganato**. Detalle completo en §Portadas de los terminales. Y la premisa "hoy nadie los mira" tampoco se sostiene: el dueño los mira en la lista, que es donde pidió verlos.
- ~~**La fase 3 se entrega en tres PRs encadenados.**~~ **Cerrado el 2026-08-20: los tres están desplegados** (PRs #27, #28 y #29, con el modal viajando encadenado bajo el #29). El primero cerró la frontera y los contratos de `intake`; el segundo agregó el escritor de repositorio, `PastedUrlIntake` y los dos endpoints; el tercero agregó el modal del frontend. `POST /api/mangas` y `POST /api/mangas/preview` existen y están en producción, así que la corrección del costo por alta de la v1.2 —**3 requests**— ya describe lo entregado y no un flujo futuro. Lo que vale conservar de esa nota es **por qué** se partió en tres y no la advertencia de que el endpoint faltaba: la frontera se entregó y se probó **antes** de que existiera el consumidor que podía violarla, que es el único orden en el que una regla direccional se prueba de verdad. El registro completo de la entrega vive en `openspec/changes/archive/panel-v1b-fase-3/`.
- **La línea de tiempo por manga está construida pero no expuesta.** `GET /api/mangas/{id}/history` y el componente `MangaTimeline` existen, tienen tests y funcionan; lo que se quitó el 2026-08-21 fue la puerta de entrada. Decisión del dueño al ver la pantalla: no le interesa leer las publicaciones de un manga contra sus propias lecturas, y el selector que llevaba ahí era además lo más feo de la pantalla. **No es código muerto a limpiar** — se conserva a propósito, porque la data se sigue pudiendo consultar por API y porque reabrir la vista más adelante debería costar un componente y no una fase. Si alguien la reabre, el punto sin resolver sigue siendo el mismo: desde dónde se llega, si desde la tarjeta del manga en la lista o desde dentro de Historial. **Va a la fase 5**, porque "desde dónde se llega" es una pregunta de navegación y ahí es donde se contesta.
- **Etapa 2 del clic en la portada: abrir el capítulo siguiente sin leer, resuelto.** La etapa 1 (§Pantallas) lleva al listado y el dueño busca su lugar a mano; esto lo lleva directo. Queda pendiente porque no es un cambio de presentación, y lo que hay que decidir es de dónde sale la respuesta. Dos cosas que la etapa 2 **no** puede asumir: que el siguiente sea `last_chapter_read + 1` —los números son decimales, la fuente publica 32.2, y hay huecos y renumeraciones, así que el siguiente es el menor capítulo mayor al leído y hay que buscarlo en una lista real—, y que `chapter_history` sirva de índice: solo guarda lo que **detectamos** y está topado por `CHAPTER_HISTORY_LIMIT`, así que para un manga leído en el 175 con 800 disponibles no contiene el 176. Las salidas son pedirle los capítulos a la fuente en el momento del clic (un request `INTERACTIVE_POLICY`, que es la clase de tráfico correcta para un clic humano) o decidir guardar la lista completa de capítulos, que es esquema nuevo. Ninguna de las dos se elige aquí. **No entra en la fase 4 ni en la 5**: una de sus dos salidas es una migración, así que es su propia entrega y su propia decisión previa, no un extra que se cuela en otra fase.
- ~~**El rechazo por duplicado o por estado terminal ofrece "Ver en «…»", que cambia de pestaña, y eso no tiene test de integración.**~~ **Cerrado el 2026-08-21.** El smoke de la fase 2 (`frontend/e2e/panel.smoke.spec.ts`, contra `tests/e2e/fixture_server.py`) cubre exactamente ese salto: un alta duplicada y terminal, clic en "Ver en «…»" y la pestaña que cambia.
- ~~**Nuevo pendiente, registrado y no resuelto aquí.** El heatmap solo excluye correcciones a la baja del cómputo; una corrección **al alza** grande (0→175) se cuenta entera como un día de lectura maratónica, sin tope del lado del servidor. No lo puede disparar un alta manual —`reading_history` solo se llena por UPDATE, un alta es un INSERT—, así que el caso real es una corrección de progreso hecha a mano. Queda expuesto para que el dueño decida si hace falta un tope, y con qué forma, una vez vea el heatmap real. **Va a la fase 4**: el heatmap real lleva desplegado desde el 2026-08-21, así que la condición que este pendiente esperaba ya se cumplió. Es cómputo del servidor, no presentación, y por eso no espera al pase de diseño.~~ **Cerrado el 2026-08-25: decisión del dueño, sin tope.** Asignarlo a la fase 4 en la v1.6 contradecía su propia decisión del 2026-08-21: el heatmap real llevaba desplegado desde ese mismo día y una corrección grande contándose entera no le pidió ningún ajuste entonces. El heatmap sigue mostrando cada corrección al alza tal cual, sin tope de ningún lado — distinguir "corrección" de "maratón genuino" pediría una señal que el esquema no guarda, para un caso que en la práctica no se disparó. **Sale de la fase 4**: deja de ser una de sus piezas (v1.7).

- **El rumbo visual del panel nunca se eligió, y es lo primero que la fase 5 tiene que cerrar.** El 2026-08-17 se prepararon tres opciones —arquitectura de información centrada en "qué tengo que leer", lista pulida con jerarquía tipográfica, y grilla de portadas— y no se decidió ninguna. Lo que se entregó desde entonces fue convergiendo hacia la tercera sin declararlo: la grilla existe, el modo oscuro existe, la pastilla "+N" existe. La fase 5 no arranca eligiendo componentes, arranca eligiendo el rumbo y escribiéndolo aquí, con el protocolo de `runbook-diseno-ui.md`.
- **El logo sigue sin empezar.** Pedido el 2026-08-17 y nunca retomado. Es el ítem más chico de la fase 5 y el único que no depende del rumbo.
- **El pase de diseño se agendó ocho días tarde, y vale registrar por qué.** El aviso existía desde el 2026-08-20 —"la fase 2 va a forzar un rework de navegación; quiero un pase de diseño, no un parche incremental"— y el runbook que dice cómo correrlo se escribió el 2026-08-21. Lo que faltaba no era método ni intención: era una línea en `docs/` que lo hiciera parte del plan. Vivía en notas de sesión, y en este repo lo que no está en `docs/` no se ejecuta, se recuerda a medias. Esa es la razón por la que la v1.6 lo convierte en fase y no en otro pendiente.

## Changelog

- **1.7 — 2026-08-25.** **Arranca la fase 4 que quedaba: portadas de terminales primero, en cuatro PRs encadenados, y esta entrega corrige tres afirmaciones que la v1.6 dejó mal — dos de dato, una de alcance.** Primero el alcance: **el tope del heatmap sale de la fase 4**, decisión del dueño y no una omisión — asignarlo ahí en la v1.6 contradecía su propia decisión del 2026-08-21, cuando el heatmap real ya llevaba desplegado y una corrección grande contándose entera no le pidió ningún ajuste; el heatmap sigue mostrando cada corrección al alza tal cual, sin tope de ningún lado, y la fase 4 queda en **cuatro piezas**: migración 3, `import-scores`, `my_score` en la lista y editable, portadas de los 66 terminales. Después los dos datos. **§`my_score`** decía que el backfill "matchea por `kitsu_id` (ya guardado en `mangas`)" y es falso: el id que trae el export (`manga_mangadb_id`) es un id de **MAL**, y hay que resolverlo contra el catálogo de Kitsu antes de poder buscar el bookmark — el mismo mecanismo que `import-kitsu`, medido en **~38 requests a Kitsu** para las 218 entradas, cero a manganato; la sección gana además el contrato de un-scoring que faltaba: a diferencia de `last_chapter_read`, `{"my_score": null}` sí puede borrar un score mal tipeado, porque no alimenta el trigger ni `reading_history`. **§Pantallas y §Portadas de los terminales** afirmaban, cada una a su modo, que un `completed`/`dropped` nunca va a tener enlace a su listado ni puede aparecer mapeado — cierto de los 66 de hoy, no un invariante: el panel edita `bookmarks.status` sin tocar `manga_sites`, así que el primer `reading` mapeado que el dueño marque `completed` conserva su mapeo, su enlace y es candidato de la ruta de portadas de terminales igual que cualquier otro. La consulta nueva (`list_stored_url_cover_candidates`) decide solo por `status`, sin `JOIN manga_sites`, justamente para no codificar esa coincidencia. Se registra por último un hueco encontrado y dejado sin cerrar: un bookmark **no terminal y sin mapeo** con un `cover_url` ya guardado no es candidato de ninguna de las dos rutas de portada — cero filas hoy, ampliar la ruta mapeada a un `LEFT JOIN` es un cambio de comportamiento a un comando ya desplegado y queda para cuando ese hueco tenga su primera fila real. Pines sin cambios, los tres re-verificados.
- **1.6 — 2026-08-25.** **El plan deja de estar incompleto: entra una quinta fase y se cierran dos pendientes que el documento arrastraba con datos equivocados.** Lo que motivó la revisión fue el dueño notando que el rediseño **no aparecía en ninguna lista de pendientes**, y tenía razón: existía el aviso (2026-08-20, "la fase 2 va a forzar un rework de navegación, quiero un pase de diseño y no un parche incremental"), existía el método (`runbook-diseno-ui.md`, escrito el 2026-08-21) y existía la constancia de que **el rumbo visual nunca se eligió** (2026-08-17, tres opciones preparadas, ninguna decidida) — pero las tres cosas vivían en notas de sesión y ninguna estaba en `docs/`. Ocho días después el rework seguía sin ocurrir aunque la fase 2 ya había aterrizado con navegación nueva. Entra por tanto la **fase 5, el pase de diseño**, con criterio de terminado propio, y se le asignan los pendientes que eran suyos y estaban sueltos: la escala del heatmap, el tono y tamaño de la pastilla "+N", la grilla por ancho de pantalla, la puerta de entrada de la línea de tiempo, `cadence_days_estimate` y el logo. **Las portadas de los terminales pasan de "y así se queda" a entrar en la fase 4**, revirtiendo la v1.0: medido contra producción el 2026-08-25, los 66 terminales ya tienen `cover_url`, los 66 apuntan a `media.kitsu.app` —el único host que no exige `Referer`— y **ninguno tiene mapeo a la fuente**, así que el costo real son 66 descargas a Kitsu y **cero requests a manganato**, y esa segunda mitad es estructural: sin slug no hay nada que preguntarle a la fuente aunque se quisiera. La regla "los terminales no consumen requests, nunca" no se toca, porque es una regla sobre la fuente y aquí no hay fuente en juego; la estimación de "~200 requests" que este documento traía describía otra operación, la de un manga mapeado sin `cover_url`. Se registra además que `list_cover_candidates` hace `JOIN manga_sites` INNER, así que **agregar `--status completed` devolvería cero filas** y hace falta una segunda ruta que baje una URL ya conocida sin pasar por el slug. Se corrige un dato de §Pantallas que estaba corto por partida doble —"59 de 229" bookmarks sin mapeo— : son **66 de 236**, y sobre todo **son exactamente los 66 terminales**, un corte limpio sobre el estado que nadie había notado y que explica por qué un `completed` nunca va a tener enlace a su listado. Se cierra el pendiente del **formato del heatmap**, que llevaba cuatro días contradiciendo a §Pantallas dentro del mismo documento: se decidió el 2026-08-21 con `/prototype` (calendario anual completo) y solo la escala sigue abierta. El **tope del heatmap para correcciones al alza** se asigna a la fase 4, porque la condición que esperaba —ver el heatmap real— se cumplió el 2026-08-21 y porque es cómputo del servidor, no presentación. La **etapa 2 del clic en la portada** se marca explícitamente fuera de la 4 y de la 5: una de sus dos salidas es una migración, así que es su propia entrega. Se fija por último la regla de orden entre la 4 y la 5: la fase 4 entrega **el dato y su edición**, sin gastar una sola decisión visual, porque el dato no lo rehace un rediseño pero su forma sí. Pines sin cambios, los tres re-verificados.
- **1.5 — 2026-08-21.** **La fase 2 se entrega completa**: los dos endpoints de lectura, la pantalla Historial y el smoke E2E con Playwright que cerraba la última deuda de tests de la fase 1. `GET /api/history/reading` agrega `reading_history` por día calendario **local** — conversión hecha en Python con `zoneinfo` sobre `LOCAL_TIMEZONE`, nunca con funciones de fecha de SQL, porque el build de SQLite de producción no trae `chr()` y su set de funciones no es confiable — y excluye del cómputo las correcciones a la baja (`chapter_num <= previous_chapter_num`); una fila con `previous_chapter_num IS NULL` (progreso que se desconocía y se corrige) suma 0 capítulos pero sí cuenta como edición. `GET /api/mangas/{id}/history` entrelaza lecturas y publicaciones cronológicamente y nunca afirma completitud: `publications_since` es el `detected_at` más antiguo conocido, no una fecha de origen. `create_app` gana un keyword-only `timezone_name` **sin default** — un default habría sido una tercera zona hardcodeada o un `UTC` silencioso, exactamente el error de clase que esta fase existe para evitar. La entrega corrige además esta misma spec: la fila de la fase 2 en §Fases decía dos deudas de tests; en realidad la del progreso en estado terminal ya se había cerrado el 2026-08-20 (PR #33, `e22309f`), y la única que seguía abierta era el Playwright, cerrado ahora (`frontend/e2e/panel.smoke.spec.ts`, contra `tests/e2e/fixture_server.py` — un servidor propio sobre una base SQLite temporal con un `MangaIntake` sin red, nunca la CLI real, para que un E2E jamás pueda alcanzar datos de producción). Dos decisiones visuales quedan deliberadamente sin resolver aquí y detrás de sus propios seams: el formato del heatmap (`domain/heatmapBuckets.ts`, sin tests a propósito) y el tono/tamaño de la pastilla "+N" (custom properties nuevas en `.behind-pill`, sin cambio de JSX). Pin sin cambios.
- **1.4 — 2026-08-21.** **El clic en la portada deja de llevar al último capítulo y lleva al listado del manga**, decisión del dueño del mismo día, entregada como etapa 1 de dos. La razón no es de gusto: el póster enlazaba a `latest_chapter_url`, que es el capítulo más nuevo, y en el caso normal de esta lista —leído hasta el 175 de 800— eso lo dejaba 625 capítulos por delante de donde quedó. La etapa 2, abrir el siguiente sin leer ya resuelto, entra en §Pendientes abiertos con las dos trampas que no puede asumir: el siguiente **no** es `last_chapter_read + 1` porque los números son decimales y hay huecos, y `chapter_history` **no** sirve de índice porque solo guarda lo detectado y está topado. Se registra además por qué la implementación no recorta la cola de `latest_chapter_url` aunque `…/manga/x/chapter-33` sin su último segmento sea la página del manga: eso arma una URL de la fuente dentro de un componente, que es la frontera estructural, y las dos direcciones dejan de coincidir en cuanto la fuente renombra un slug. No hacía falta derivar nada — `manga_sites.url` ya guardaba la página desde la primera versión del alta, con las 170 filas mapeadas de producción pobladas y ninguna nula, así que la entrega es una columna más en el `SELECT` del panel y **cero requests nuevos**. Efecto lateral buscado: el enlace ahora depende del mapeo a la fuente y no de una detección, así que un manga recién dado de alta ya enlaza a su listado. La tabla de API gana `manga_url` y el Resumen una fila de destino del clic. Sin cambios de pines.
- **1.3 — 2026-08-20.** **El documento se pone al día con la fase 3, desplegada ese día** (PRs #27, #28 y #29, con el modal encadenado bajo el #29). Se cierran dos pendientes abiertos que la entrega volvió falsos: el sellado de `status_changed_at` al crear el bookmark —que decía "no hay nada que hacer hoy: ese formulario no existe", y hoy `write_manual_add` lo sella en el mismo `INSERT`— y la nota de los tres PRs encadenados, que afirmaba que `POST /api/mangas` y `POST /api/mangas/preview` "no existen todavía". De esa segunda nota se conserva lo que sigue sirviendo como historia: la frontera se entregó y se probó antes de que existiera el consumidor capaz de violarla, que es el único orden en el que una regla direccional se prueba de verdad. Se corrige además **un error de nombre que este documento cargó desde la v1.0 en tres lugares** —§La frontera, la tabla de API y la tabla de fases— y que la exploración de la fase 3 identificó como trampa: el alta **no** valida el slug "vía `catalogue`". `catalogue` es Kitsu detrás de un contrato que no menciona manganato y no sabe qué es un slug; la capa que la fase 3 construyó es `intake/`, y la regla direccional entregada le prohíbe a `web` importar `catalogue` justamente. La tabla de API gana las dos rutas que faltaban (`POST /api/mangas/preview` y `GET /api/mangas/preview-cover`, esta última no prevista: sin proxy toda vista previa mostraba el placeholder, porque los hosts de imágenes responden 403 al hotlink). La tabla de fases gana el estado de las cuatro, el Resumen gana una fila de estado, y §Pantallas registra que el alta se entregó **como modal sobre la lista y no como pantalla propia** (decisión del dueño del 2026-08-19), así que las pantallas siguen siendo dos. Se registra por último que **el orden de fases se saltó dos veces** —las portadas primero, la fase 3 después— y se lo reclasifica como preferencia por defecto en vez de restricción, con la razón por la que no costó nada: las fases del panel no se apoyan unas en otras salvo la 4, que necesita la migración 3. Lo que la fase 2 sí arrastra por postergarse es su deuda de tests, abierta desde la verificación de la fase 1. Nuevo pendiente: la afordancia "Ver en «…»" del rechazo por duplicado no tiene test de integración y espera al smoke E2E de la fase 2.
- **1.2 — 2026-08-19.** Se corrige el costo por alta de la fase 3 en el resumen: **3 requests**, no 1 — la ficha para la vista previa, los capítulos y la portada al confirmar. La v1.1 databa de antes de la decisión de cachear la portada durante el alta (§128, entregada el 2026-08-18); el número original nunca contó ese tercer request. Se agrega una nota en pendientes abiertos documentando que la fase 3 llega en tres PRs encadenados y que el endpoint todavía no existe mientras solo el primero está desplegado.
- **1.1 — 2026-08-18.** El documento se pone al día con lo entregado sobre la fase 1 y con lo que se adelantó de la fase 4. Lo nuevo: el **orden por pestaña** (`last_read_at` en "Leyendo", `status_changed_at` en "En pausa", desconocidos al final por título, calculado en el cliente porque el orden es de la pestaña y no del request) y la columna "Última lectura" **sin hora**, con el tooltip de publicación conservándola. Se corrige un supuesto de la v1.0 que la medición del 2026-08-18 tumbó: guardar `cover_url` **no** alcanza, porque los hosts de imágenes de la fuente responden 403 sin su propio `Referer`, así que se cachea la imagen y el panel sirve los bytes; el costo real es de ~180 requests (35 del tab "Leyendo" y 145 del resto), del mismo orden que los ~230 estimados: lo que cambió no es el número sino su forma, porque 212 de 229 mangas ya traían URL del import y solo hace falta bajar la imagen. Y se corrige un **choque de numeración**: la migración 2 la tomó `bookmarks.status_changed_at`, así que `my_score` pasa a ser la 3 — un número desplegado está grabado en el `user_version` de cada base y no se reutiliza. Se registra además la dirección visual que el dueño eligió el mismo día —grilla de portadas en vez de tabla, insignia de atraso como pastilla "+N"— **ya entregada** en `774cdcf`, junto con la razón por la que la prosa "Vas atrasado N capítulos" no pagaba su espacio: se dispara en 18 de 18 filas. Pin actualizado: `spec-modelo-de-datos.md` v1.8 → v1.9.
- **1.0 — 2026-08-17.** Spec inicial. Cierra las tres decisiones que `decision-arquitectura-v1b.md` dejó abiertas: FastAPI, sin autenticación, y el alcance funcional en cuatro fases con la edición de progreso como corazón. Alcance acordado con el dueño el mismo día: estados, historial/heatmap, alta/baja y portadas entran todos, ordenados por fase; `my_score` se recupera en la fase 4 con la migración 2. La topología también es del dueño: panel en contenedor propio — aislamiento sobre minimalismo, un panel caído no tumba la detección.
