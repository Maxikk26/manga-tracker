"""The panel's presentation layer (docs/spec-panel-v1b.md): JSON endpoints
under /api plus the static frontend. Shows and edits, never detects or
notifies — `web` imports `storage` and nothing source- or Telegram-shaped
(DIRECTIONAL_RULES in tests/test_architecture.py)."""
